import { NextResponse } from "next/server";
import { PDFDocument, StandardFonts } from "pdf-lib";
import { requireSchoolSession } from "@/lib/auth";
import { withTenant } from "@/lib/db";
import { requirePermission } from "@/lib/rbac";
import { routeError } from "@/lib/errors";

type Row={type:string;date:Date;party:string;category:string;method:string;reference:string;status:string;amount:unknown};
const esc=(v:unknown)=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;");
const csv=(v:unknown)=>`"${String(v??"").replaceAll('"','""')}"`;
const date=(v:Date)=>v.toISOString().slice(0,10);
const money=(v:unknown)=>Number(v||0).toFixed(2);
function range(url:URL){const from=url.searchParams.get("from")||null;const to=url.searchParams.get("to")||null;return{from:from?new Date(`${from}T00:00:00.000Z`):null,to:to?new Date(`${to}T23:59:59.999Z`):null};}
async function pdfBytes(schoolName:string,rows:Row[],from:Date|null,to:Date|null){const pdf=await PDFDocument.create();const font=await pdf.embedFont(StandardFonts.Helvetica);const bold=await pdf.embedFont(StandardFonts.HelveticaBold);let page=pdf.addPage([595,842]);let y=800;const line=(text:string,size=9,isBold=false)=>{if(y<55){page=pdf.addPage([595,842]);y=800;}page.drawText(text.slice(0,105),{x:42,y,size,font:isBold?bold:font});y-=size+6;};line(schoolName,16,true);line("Finance Transaction Register",13,true);line(`Period: ${from?date(from):"Beginning"} to ${to?date(to):"Latest"}`,9);y-=8;for(const r of rows)line(`${date(r.date)} | ${r.type} | ${r.party} | ${r.category} | GH₵${money(r.amount)} | ${r.status}`,8);return pdf.save();}
export async function GET(request:Request){try{const session=await requireSchoolSession();const url=new URL(request.url);const format=(url.searchParams.get("format")||"csv").toLowerCase();if(!["csv","xls","doc","pdf","json"].includes(format))return NextResponse.json({error:"Unsupported export format."},{status:400});const{from,to}=range(url);const result=await withTenant(session.schoolId,async tx=>{await requirePermission(tx,session.userId,"finance:export");const school=await tx.school.findUnique({where:{id:session.schoolId},select:{name:true}});const payments=await tx.$queryRawUnsafe<any[]>(`SELECT 'Payment' AS type,p."createdAt" AS date,s."name" AS party,COALESCE(string_agg(DISTINCT fc."name",', '),'Fees') AS category,p."method" AS method,COALESCE(p."reference",p."id") AS reference,CASE WHEN COALESCE(SUM(pr."amount"),0)>=p."amount" THEN 'reversed' WHEN COALESCE(SUM(pr."amount"),0)>0 THEN 'partially reversed' ELSE 'posted' END AS status,p."amount"-COALESCE(SUM(pr."amount"),0) AS amount FROM "Payment" p JOIN "Invoice" i ON i."id"=p."invoiceId" AND i."schoolId"=p."schoolId" JOIN "Student" s ON s."id"=i."studentId" AND s."schoolId"=i."schoolId" LEFT JOIN "FinancePaymentAllocation" a ON a."paymentId"=p."id" AND a."schoolId"=p."schoolId" LEFT JOIN "FinanceStudentCharge" ch ON ch."id"=a."chargeId" AND ch."schoolId"=a."schoolId" LEFT JOIN "FinanceFeeCategory" fc ON fc."id"=ch."categoryId" AND fc."schoolId"=ch."schoolId" LEFT JOIN "PaymentReversal" pr ON pr."paymentId"=p."id" AND pr."schoolId"=p."schoolId" WHERE p."schoolId"=$1 AND ($2::timestamp IS NULL OR p."createdAt">=$2) AND ($3::timestamp IS NULL OR p."createdAt"<=$3) GROUP BY p."id",s."name" ORDER BY p."createdAt"`,session.schoolId,from,to);const expenses=await tx.$queryRawUnsafe<any[]>(`SELECT 'Expense' AS type,e."expenseDate"::timestamp AS date,e."vendor" AS party,e."category",e."paymentMethod" AS method,COALESCE(e."reference",e."id") AS reference,e."status",CASE WHEN e."status"='reversed' THEN 0 ELSE -e."amount" END AS amount FROM "FinanceExpense" e WHERE e."schoolId"=$1 AND ($2::date IS NULL OR e."expenseDate">=$2::date) AND ($3::date IS NULL OR e."expenseDate"<=$3::date) ORDER BY e."expenseDate"`,session.schoolId,from,to);return{schoolName:school?.name||"School",rows:[...payments,...expenses].sort((a,b)=>new Date(a.date).getTime()-new Date(b.date).getTime()) as Row[]};});const filename=`finance-${new Date().toISOString().slice(0,10)}`;if(format==="json")return new NextResponse(JSON.stringify({school:result.schoolName,from,to,transactions:result.rows},null,2),{headers:{"content-type":"application/json; charset=utf-8","content-disposition":`attachment; filename="${filename}.json"`}});if(format==="csv"){const body=["Date,Type,Party,Category,Method,Reference,Status,Amount (GHS)",...result.rows.map(r=>[date(r.date),r.type,r.party,r.category,r.method,r.reference,r.status,money(r.amount)].map(csv).join(","))].join("\n");return new NextResponse(body,{headers:{"content-type":"text/csv; charset=utf-8","content-disposition":`attachment; filename="${filename}.csv"`}});}if(format==="pdf"){const bytes=await pdfBytes(result.schoolName,result.rows,from,to);return new NextResponse(Buffer.from(bytes),{headers:{"content-type":"application/pdf","content-disposition":`attachment; filename="${filename}.pdf"`}});}const html=`<!doctype html><html><head><meta charset="utf-8"><title>Finance report</title><style>body{font-family:Arial,sans-serif}h1{margin-bottom:4px}table{border-collapse:collapse;width:100%}th,td{border:1px solid #999;padding:7px;text-align:left}th{font-weight:bold}</style></head><body><h1>${esc(result.schoolName)}</h1><p>Finance transaction register</p><table><thead><tr><th>Date</th><th>Type</th><th>Party</th><th>Category</th><th>Method</th><th>Reference</th><th>Status</th><th>Amount (GHS)</th></tr></thead><tbody>${result.rows.map(r=>`<tr><td>${date(r.date)}</td><td>${esc(r.type)}</td><td>${esc(r.party)}</td><td>${esc(r.category)}</td><td>${esc(r.method)}</td><td>${esc(r.reference)}</td><td>${esc(r.status)}</td><td>${money(r.amount)}</td></tr>`).join("")}</tbody></table></body></html>`;const ext=format==="xls"?"xls":"doc";const type=format==="xls"?"application/vnd.ms-excel":"application/msword";return new NextResponse(html,{headers:{"content-type":`${type}; charset=utf-8`,`content-disposition`:`attachment; filename="${filename}.${ext}"`}});}catch(error){return routeError(error);}}
