import Link from "next/link";
import { AppShell } from "@/components/AppShell";
import { AcademicWorkspaceNav } from "@/components/AcademicWorkspaceNav";
import { requireSchoolSession } from "@/lib/school-auth";
import { withTenant } from "@/lib/db";
import { ForbiddenError } from "@/lib/errors";
import { hasPermission } from "@/lib/rbac";
import "./exams-workspace.css";
import "../academic-workspace.css";
export default async function ExamsPage(){const session=await requireSchoolSession();const data=await withTenant(session.schoolId,async tx=>{const [canManage,canReport,canTeacher,canWriteAssigned,canWriteAll]=await Promise.all([hasPermission(tx,session.userId,"exams:manage"),hasPermission(tx,session.userId,"reports:generate"),hasPermission(tx,session.userId,"exams:take"),hasPermission(tx,session.userId,"scores:write:assigned"),hasPermission(tx,session.userId,"scores:write:all")]);if(!canManage&&!canReport&&!canTeacher&&!canWriteAssigned&&!canWriteAll)throw new ForbiddenError("You do not have access to assessment records.");const[assessments,terms,classes]=await Promise.all([tx.assessment.findMany({where:{schoolId:session.schoolId},select:{id:true,name:true,maxScore:true,term:{select:{name:true}},subject:{select:{name:true}},class:{select:{name:true}}},take:30}),tx.term.findMany({where:{schoolId:session.schoolId},select:{id:true,name:true,academicYear:{select:{name:true}}},orderBy:{startDate:"desc"},take:8}),tx.class.count({where:{schoolId:session.schoolId}})]);return{canManage,canReport,canTeacher,assessments,terms,classes}});return <AppShell universe="school" title="Exams & Assessments" subtitle="Build assessment evidence inside the academic model." active="Exams & Assessments"><div className="academic-page"><section className="academic-page-hero"><div className="academic-page-hero-copy"><span className="academic-page-overline">ASSESSMENTS · EVIDENCE</span><h1>Assessment should follow the class, subject and term—not sit beside them.</h1><p>Every assessment belongs to a class, subject and academic term. Those records feed Gradebook, Performance and the report-card chain.</p></div><div className="academic-page-hero-side"><div className="academic-page-actions"><Link className="academic-btn-secondary" href="/school/academics/setup">Assessment rules</Link><Link className="academic-btn-secondary" href="/school/terms">Terms & calendar</Link><Link className="academic-btn-secondary" href="/school/gradebook/studio">Gradebook</Link></div></div></section><AcademicWorkspaceNav current="assessments"/><div className="exams-shell"><section className="exams-hero"><div><span className="exams-kicker">ASSESSMENT OPERATIONS</span><h2>Assessment should follow the programme, not sit beside it.</h2></div><div className="exams-actions"><Link className="exams-primary" href="/school/gradebook/studio">Gradebook Studio →</Link><Link className="exams-secondary" href="/school/academics/setup">Assessment rules</Link></div></section><section className="exams-kpis"><article><span>Assessment records</span><strong>{data.assessments.length}</strong><small>Recent school assessments</small></article><article><span>Academic terms</span><strong>{data.terms.length}</strong><small>Available scheduling periods</small></article><article><span>Classes</span><strong>{data.classes}</strong><small>School class groups</small></article><article><span>Your access</span><strong>{data.canManage?"Manage":data.canReport?"Review":"Teaching"}</strong><small>{data.canReport?"Leadership oversight":"Assignment-based access"}</small></article></section><section className="exams-card"><div className="exams-card-head"><div><span className="exams-kicker">RECENT ASSESSMENTS</span><h3>Assessment register</h3></div></div><div className="assessment-register">{data.assessments.length===0?<div className="exams-empty">No assessment records yet. Start from the school's assessment structure or create the first classroom assessment.</div>:data.assessments.map(a=><article key={a.id}><div><b>{a.name}</b><small>{a.class?.name??"Unlinked class"} · {a.subject?.name??"Unlinked subject"} · {a.term?.name??"No term"}</small></div><span>{String(a.maxScore)} max</span></article>)}</div></section><section className="exams-flow"><div><span className="exams-kicker">ACADEMIC CONTROL CHAIN</span><h3>Programme → assessment → marks → moderation → report</h3></div><div className="exams-links"><Link href="/school/lessons">Lesson plans <span>→</span></Link><Link href="/school/homework">Homework <span>→</span></Link><Link href="/school/gradebook">Gradebook <span>→</span></Link><Link href="/school/report-cards">Report cards <span>→</span></Link></div></section></div></div></AppShell>}
