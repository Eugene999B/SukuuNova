import Link from "next/link";
import { AppShell } from "@/components/AppShell";
import { AcademicWorkspaceNav } from "@/components/AcademicWorkspaceNav";
import { requireSchoolSession } from "@/lib/school-auth";
import { withTenant } from "@/lib/db";
import { hasPermission } from "@/lib/rbac";
import { getGradebookConfiguration, getClassSubjectPerformance } from "@/lib/academic-engine";
import { isTermActive } from "@/lib/term-date";
import GradebookEntryGrid from "@/components/GradebookEntryGrid";
import "../../academic-workspace.css";
export default async function GradebookStudioPage({ searchParams }: { searchParams: Promise<{ class?: string; subject?: string; term?: string }> }) {
  const session = await requireSchoolSession(); const params = await searchParams;
  const data = await withTenant(session.schoolId, async (tx) => {
    const canWriteAssigned = await hasPermission(tx, session.userId, "scores:write:assigned"); const canWriteAll = await hasPermission(tx, session.userId, "scores:write:all");
    if (!canWriteAssigned && !canWriteAll) throw new Error("You do not have gradebook access.");
    const [school, config, settings] = await Promise.all([
      tx.school.findUnique({ where: { id: session.schoolId }, select: { name: true, uniqueCode: true } }),
      getGradebookConfiguration(tx),
      tx.schoolSettings.findUnique({ where: { schoolId: session.schoolId }, select: { timezone: true } }),
    ]);
    const assignments = config.assignments.filter((item) => canWriteAll || item.teacherId === session.userId);
    const selectedClass = params.class || ""; const selectedSubject = params.subject || ""; const timezone = settings?.timezone || "Africa/Accra";
    const selectedTerm = config.terms.find((item) => item.id === params.term) ?? config.terms.find((item) => isTermActive(item, new Date(), timezone)) ?? null;
    const assignment = assignments.find((item) => item.classId === selectedClass && item.subjectId === selectedSubject) ?? null;
    const performance = assignment && selectedTerm ? await getClassSubjectPerformance(tx, assignment.classId, assignment.subjectId, selectedTerm.id) : null;
    return { school, config, assignments, assignment, selectedTerm, performance, canWriteAll };
  });
  const classes = Array.from(new Map(data.assignments.map((item) => [item.classId, item.class])).values()).sort((a, b) => `${a.level ?? ""}${a.name}`.localeCompare(`${b.level ?? ""}${b.name}`));
  const subjects = Array.from(new Map(data.assignments.filter((item) => !params.class || item.classId === params.class).map((item) => [item.subjectId, item.subject])).values()).sort((a, b) => a.name.localeCompare(b.name));
  const terms = data.config.terms; const contextQuery = data.assignment && data.selectedTerm ? `?class=${encodeURIComponent(data.assignment.classId)}&subject=${encodeURIComponent(data.assignment.subjectId)}&term=${encodeURIComponent(data.selectedTerm.id)}` : ""; const rows = data.performance?.rows ?? [];
  const marked = rows.reduce((count, row) => count + row.scores.filter((score) => score.rawScore != null || score.status === "excused").length, 0); const totalCells = (data.performance?.assessments.length ?? 0) * rows.length; const completion = totalCells ? Math.round((marked / totalCells) * 100) : 0; const termLabel = data.selectedTerm?.name ?? "No active term selected"; const classLabel = data.assignment ? `${data.assignment.class.level ? `${data.assignment.class.level} · ` : ""}${data.assignment.class.name}` : "Choose a class"; const subjectLabel = data.assignment?.subject.name ?? "Choose a subject";
  return <AppShell universe="school" title="Gradebook" subtitle="Enter and moderate marks." active="Gradebook" schoolName={data.school?.name ?? "School Workspace"} schoolCode={data.school?.uniqueCode ?? ""} userName={session.name}><div className="academic-page"><section className="academic-page-hero"><div className="academic-page-hero-copy"><span className="academic-page-overline">GRADEBOOK · MARK ENTRY</span><h1>Enter marks in the same context the rest of the school uses.</h1><p>Choose one class, subject and term. SukuuNova then opens the assessments, learners and weighted result for that exact teaching assignment.</p></div><div className="academic-page-hero-side"><div className="academic-page-context"><span className="academic-context-chip"><strong>Class</strong> {classLabel}</span><span className="academic-context-chip"><strong>Subject</strong> {subjectLabel}</span><span className="academic-context-chip"><strong>Term</strong> {termLabel}</span></div><div className="academic-page-actions"><Link className="academic-btn-secondary" href="/school/gradebook">Results hub</Link><Link className="academic-btn-secondary" href="/school/academics/setup">Academic setup</Link></div></div></section><AcademicWorkspaceNav current="gradebook"/><section className="academic-step-row"><div className="academic-step-card"><span className="academic-step-number">01</span><div><strong>Configure</strong><small>Assessment and term rules</small></div></div><div className="academic-step-card"><span className="academic-step-number">02</span><div><strong>Enter</strong><small>Record mark and status</small></div></div><div className="academic-step-card"><span className="academic-step-number">03</span><div><strong>Review</strong><small>See weighted class results</small></div></div><div className="academic-step-card"><span className="academic-step-number">04</span><div><strong>Report</strong><small>Feed approved results forward</small></div></div></section><section className="academic-context-card"><div className="academic-section-head"><div><span className="academic-page-overline">WORKING CONTEXT</span><h2>Choose the teaching assignment</h2><p>The term comes from the school calendar, while class and subject come from the teacher assignment model.</p></div></div><form className="academic-context-form" action="/school/gradebook/studio" method="get"><div className="academic-field"><label htmlFor="gradebook-class">Class</label><select id="gradebook-class" name="class" defaultValue={params.class ?? ""}><option value="">Choose class</option>{classes.map((item) => <option key={item.id} value={item.id}>{item.level ? `${item.level} · ` : ""}{item.name}</option>)}</select></div><div className="academic-field"><label htmlFor="gradebook-subject">Subject</label><select id="gradebook-subject" name="subject" defaultValue={params.subject ?? ""}><option value="">Choose subject</option>{subjects.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div className="academic-field"><label htmlFor="gradebook-term">Term</label><select id="gradebook-term" name="term" defaultValue={data.selectedTerm?.id ?? ""}><option value="">Choose term</option>{terms.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><button className="academic-context-submit" type="submit">Open gradebook</button></form></section>{!data.assignment || !data.performance ? <section className="academic-empty"><strong>{data.selectedTerm ? "Start with a valid class + subject assignment." : "Choose an academic term before entering marks."}</strong><p>{data.selectedTerm ? "The selected class and subject must be connected through Classes & Houses before marks can be entered." : "There is no active academic term right now. Choose a term explicitly so marks cannot accidentally be entered against a completed or future period."}</p><div className="academic-empty-actions"><Link href="/school/classes">Manage class assignments</Link><Link href="/school/exams">Open assessments</Link><Link href="/school/terms">Check term calendar</Link></div></section> : <><section className="academic-stat-row"><div className="academic-stat"><span>Learners</span><strong>{rows.length}</strong><small>Active learners in {classLabel}</small></div><div className="academic-stat"><span>Assessments</span><strong>{data.performance.assessments.length}</strong><small>Configured for {termLabel}</small></div><div className="academic-stat"><span>Entry completion</span><strong>{completion}%</strong><small>{marked} of {totalCells} mark cells recorded</small></div><div className="academic-stat"><span>Working term</span><strong>{data.selectedTerm?.name ?? "—"}</strong><small>Inherited from the academic calendar</small></div></section><section className="academic-main-grid"><div className="academic-work-card"><div className="academic-section-head"><div><span className="academic-page-overline">02 · ENTER & MODERATE</span><h2 id="gradebook-entry-heading">{subjectLabel} · {classLabel}</h2><p>Marks save against the current school assignment and feed the weighted result shown in Performance Studio.</p></div><Link className="academic-btn-secondary" href={`/school/academics/performance${contextQuery}`}>View performance</Link></div>{data.performance.assessments.length === 0 ? <div className="academic-empty"><strong>No assessments are configured for this term.</strong><p>Set the assessment structure first; the same configuration will be used for grade calculations and report cards.</p><div className="academic-empty-actions"><Link href="/school/exams">Configure assessments</Link><Link href="/school/academics/setup">Review grading rules</Link></div></div> : <GradebookEntryGrid assessments={data.performance.assessments} rules={{ categories: data.config.assessment.categories, rounding: data.config.assessment.rounding, missingScorePolicy: data.config.assessment.missingScorePolicy }} rows={rows.map((row) => ({ student: row.student, total: row.total, scores: row.scores.map((score) => ({ assessmentId: score.assessmentId, rawScore: score.rawScore, maxScore: score.maxScore, status: (score as { status?: string }).status ?? null })) }))} />}</div><aside className="academic-side-stack"><div className="academic-side-card"><span className="academic-page-overline">03 · RESULT RULE</span><strong>Weighted subject result</strong><small>These weights come directly from Academic Setup. There is no second grading definition inside the gradebook.</small><div className="academic-link-list">{data.config.assessment.categories.map((category) => <div key={category.name}><span>{category.name}</span><strong>{category.weight}%</strong></div>)}</div></div><div className="academic-side-card"><span className="academic-page-overline">NEXT</span><strong>Move the same context forward</strong><small>Performance reads the same class, subject and term. Report cards then use the approved result chain.</small><div className="academic-link-list"><Link href={`/school/academics/performance${contextQuery}`}>Performance <span>→</span></Link><Link href="/school/report-cards">Report cards <span>→</span></Link><Link href="/school/academics/health">Academic readiness <span>→</span></Link></div></div></aside></section></>}}</div></AppShell>;
}