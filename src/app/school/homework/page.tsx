"use client";
import Link from "next/link";
import { useEffect,useMemo,useState } from "react";
import { AppShell } from "@/components/AppShell";
import "./homework-workspace.css";
type Assignment={classId:string;subjectId:string;class:{name:string;level:string|null};subject:{name:string}};type Row={id:string;teacherName:string;className:string;subjectName:string;termName:string|null;title:string;instructions:string;dueDate:string;points:number|null;assignmentStatus:string;reviewStatus:string;reviewNote:string|null};type Term={id:string;name:string;academicYear:{name:string}};type Data={rows:Row[];assignments:Assignment[];terms:Term[];review:boolean;manage:boolean};
export default function HomeworkPage(){const[data,setData]=useState<Data|null>(null),[tab,setTab]=useState("all"),[open,setOpen]=useState(false),[message,setMessage]=useState(""),[saving,setSaving]=useState(false);const[form,setForm]=useState({classId:"",subjectId:"",termId:"",title:"",instructions:"",dueDate:new Date(Date.now()+86400000).toISOString().slice(0,10),points:"",assignmentStatus:"draft"});const load=()=>fetch("/api/school/homework").then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to load homework.");return d as Data}).then(setData).catch(e=>setMessage(e instanceof Error?e.message:"Unable to load homework."));useEffect(()=>{void load()},[]);const filtered=useMemo(()=>{const rows=data?.rows??[];if(tab==="all")return rows;if(tab==="review")return rows.filter(r=>r.reviewStatus==="not_reviewed"&&r.assignmentStatus!=="draft");return rows.filter(r=>r.assignmentStatus===tab)},[data,tab]);const counts={all:data?.rows.length??0,draft:data?.rows.filter(r=>r.assignmentStatus==="draft").length??0,assigned:data?.rows.filter(r=>r.assignmentStatus==="assigned").length??0,closed:data?.rows.filter(r=>r.assignmentStatus==="closed").length??0,review:data?.rows.filter(r=>r.reviewStatus==="not_reviewed"&&r.assignmentStatus!=="draft").length??0};const save=async()=>{setSaving(true);setMessage("");try{const r=await fetch("/api/school/homework",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...form,points:form.points?Number(form.points):undefined})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Could not save homework.");setMessage(form.assignmentStatus==="assigned"?"Homework assigned to the selected class.":"Homework saved as draft.");setOpen(false);setForm({...form,title:"",instructions:""});await load()}catch(e){setMessage(e instanceof Error?e.message:"Could not save homework.")}finally{setSaving(false)}};const review=async(id:string,status:"approved"|"changes_requested")=>{setSaving(true);try{const note=status==="changes_requested"?window.prompt("What should the teacher change?","")||"Please revise this exercise.":"Exercise reviewed and approved.";const r=await fetch("/api/school/homework",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,reviewStatus:status,reviewNote:note})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Could not review homework.");setMessage(status==="approved"?"Homework approved.":"Changes requested from the teacher.");await load()}catch(e){setMessage(e instanceof Error?e.message:"Review failed")}finally{setSaving(false)}};return <AppShell universe="school" title="Homework & Exercises" subtitle="Teachers create work for their assigned classes; academic leaders review quality and monitor delivery." active="Homework & Exercises"><div className="homework-shell"><section className="homework-hero"><div><span className="homework-kicker">CLASSROOM WORK</span><h2>{data?.review&&!data.manage?"Review and verify assigned work":"Create purposeful work for your learners"}</h2><p>{data?.review&&!data.manage?"Check that submitted exercises match the subject, class level and current teaching plan before they reach learners.":"Homework is tied to the teacher's actual class and subject assignment, keeping classroom ownership clear."}</p></div>{data?.manage?<button className="homework-primary" onClick={()=>setOpen(true)}>＋ New exercise</button>:null}</section><section className="homework-kpis"><article><span>Total</span><strong>{counts.all}</strong><small>Visible homework</small></article><article><span>Drafts</span><strong>{counts.draft}</strong><small>Teacher working copies</small></article><article><span>Assigned</span><strong>{counts.assigned}</strong><small>Released to class</small></article><article><span>Review queue</span><strong>{counts.review}</strong><small>Needs academic review</small></article></section><section className="homework-card"><div className="homework-tabs"><button className={tab==="all"?"active":""} onClick={()=>setTab("all")}>All <b>{counts.all}</b></button><button className={tab==="draft"?"active":""} onClick={()=>setTab("draft")}>Draft <b>{counts.draft}</b></button><button className={tab==="assigned"?"active":""} onClick={()=>setTab("assigned")}>Assigned <b>{counts.assigned}</b></button><button className={tab==="closed"?"active":""} onClick={()=>setTab("closed")}>Closed <b>{counts.closed}</b></button>{data?.review?<button className={tab==="review"?"active":""} onClick={()=>setTab("review")}>Review queue <b>{counts.review}</b></button>:null}</div><div className="homework-list">{filtered.length===0?<div className="homework-empty"><h3>No homework in this view</h3><p>{data?.manage?"Create the next class exercise from your assigned teaching load.":"Teacher-created exercises awaiting your review will appear here."}</p></div>:filtered.map(row=><article className="homework-row" key={row.id}><div><span>{row.className} · {row.subjectName}</span><h3>{row.title}</h3><p>{row.instructions}</p><small>{row.teacherName} · due {new Date(row.dueDate).toLocaleDateString("en-GB")}{row.points!=null?` · ${row.points} points`:""}</small></div><div className="homework-side"><span className={`homework-status ${row.assignmentStatus}`}>{row.assignmentStatus}</span><span className={`review-badge ${row.reviewStatus}`}>{row.reviewStatus.replace("_"," ")}</span>{data?.review&&row.reviewStatus==="not_reviewed"?<div className="homework-review"><button onClick={()=>void review(row.id,"approved")} disabled={saving}>Approve</button><button onClick={()=>void review(row.id,"changes_requested")} disabled={saving}>Changes</button></div>:null}</div></article>)}</div></section><section className="homework-card homework-chain"><div><span className="homework-kicker">CONNECTED ACADEMIC FLOW</span><h3>Lesson plan → exercise → learner work → gradebook</h3><p>Homework sits alongside lesson planning and results, so leadership can verify whether assigned work matches the teaching programme and teachers can move from planning into evidence of learning.</p></div><div className="homework-links"><Link href="/school/lessons">Lessons & planning <span>→</span></Link><Link href="/school/gradebook/studio">Gradebook Studio <span>→</span></Link><Link href="/school/academics/performance">Performance Studio <span>→</span></Link><Link href="/school/report-cards">Report cards <span>→</span></Link></div></section>{open&&<div className="homework-modal-backdrop"><section className="homework-modal"><div className="homework-modal-head"><div><span className="homework-kicker">NEW EXERCISE</span><h3>Create work for an assigned class</h3></div><button onClick={()=>setOpen(false)}>×</button></div><div className="homework-form-grid"><label>Class<select value={form.classId} onChange={e=>setForm({...form,classId:e.target.value,subjectId:""})}><option value="">Choose class…</option>{data?.assignments.filter((a,i,self)=>self.findIndex(x=>x.classId===a.classId)===i).map(a=><option key={a.classId} value={a.classId}>{a.class.level?`${a.class.level} · `:""}{a.class.name}</option>)}</select></label><label>Subject<select value={form.subjectId} onChange={e=>setForm({...form,subjectId:e.target.value})}><option value="">Choose subject…</option>{data?.assignments.filter(a=>!form.classId||a.classId===form.classId).map(a=><option key={`${a.classId}:${a.subjectId}`} value={a.subjectId}>{a.subject.name}</option>)}</select></label><label>Term<select value={form.termId} onChange={e=>setForm({...form,termId:e.target.value})}><option value="">No term</option>{data?.terms.map(t=><option key={t.id} value={t.id}>{t.name} · {t.academicYear.name}</option>)}</select></label><label>Due date<input type="date" value={form.dueDate} onChange={e=>setForm({...form,dueDate:e.target.value})}/></label><label>Points<input type="number" min="0" value={form.points} onChange={e=>setForm({...form,points:e.target.value})} placeholder="Optional"/></label><label className="wide">Title<input value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="e.g. Practice set: equivalent fractions"/></label><label className="wide">Instructions<textarea rows={8} value={form.instructions} onChange={e=>setForm({...form,instructions:e.target.value})} placeholder="What learners should complete, submit or discuss…"/></label></div><div className="homework-modal-actions"><button className="homework-secondary" onClick={()=>{setForm({...form,assignmentStatus:"draft"});setTimeout(()=>void save(),0)}} disabled={saving}>Save draft</button><button className="homework-primary" onClick={()=>{setForm({...form,assignmentStatus:"assigned"});setTimeout(()=>void save(),0)}} disabled={saving}>Assign exercise</button></div></section></div>}{message?<div className="homework-message">{message}</div>:null}</div></AppShell>}
