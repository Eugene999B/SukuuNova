"use client";

import Link from "next/link";
import { useEffect, useRef, useState } from "react";

type SignatureSlot={role:string;name:string;signatureDataUrl?:string};
type Config={classAssessmentWeight:number;examWeight:number;classAssessmentTypes:string[];examTypes:string[];rounding:"nearest"|"down"|"up";missingScorePolicy:"blank"|"zero";showStudentPhoto:boolean;showOverallPosition:boolean;showSubjectPosition:boolean;showAttendance:boolean;showPromotion:boolean;showClassTeacherRemark:boolean;showHeadteacherRemark:boolean;signatureSlots:SignatureSlot[]};

const fallback:Config={classAssessmentWeight:30,examWeight:70,classAssessmentTypes:["Exercise","Homework","Participation","Quiz","Project","Classwork"],examTypes:["Exam","Examination"],rounding:"nearest",missingScorePolicy:"blank",showStudentPhoto:true,showOverallPosition:true,showSubjectPosition:true,showAttendance:true,showPromotion:true,showClassTeacherRemark:true,showHeadteacherRemark:true,signatureSlots:[{role:"Class Teacher",name:""},{role:"Headteacher",name:""}]};

export default function ReportCardIntelligenceSettings(){
 const [config,setConfig]=useState<Config>(fallback); const [message,setMessage]=useState(""); const [busy,setBusy]=useState(false);
 useEffect(()=>{fetch("/api/school/settings/reporting/config",{cache:"no-store"}).then(async r=>{const j=await r.json();if(r.ok)setConfig({...fallback,...j});}).catch(()=>{});},[]);
 const save=async()=>{setBusy(true);setMessage("");try{if(Math.abs(Number(config.classAssessmentWeight)+Number(config.examWeight)-100)>0.001)throw new Error("Class assessment and examination weights must total 100%.");const r=await fetch("/api/school/settings/reporting/config",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(config)});const j=await r.json();if(!r.ok)throw new Error(j.error??"Unable to save");setMessage("Report card intelligence settings saved.");}catch(e){setMessage(e instanceof Error?e.message:"Unable to save settings.");}finally{setBusy(false);}};
 const updateList=(key:"classAssessmentTypes"|"examTypes",i:number,value:string)=>setConfig(c=>({...c,[key]:c[key].map((v,n)=>n===i?value:v)}));
 const addType=(key:"classAssessmentTypes"|"examTypes")=>setConfig(c=>({...c,[key]:[...c[key],key==="classAssessmentTypes"?"New assessment":"New exam"]}));
 const removeType=(key:"classAssessmentTypes"|"examTypes",i:number)=>setConfig(c=>({...c,[key]:c[key].filter((_,n)=>n!==i)}));
 const updateSig=(i:number,patch:Partial<SignatureSlot>)=>setConfig(c=>({...c,signatureSlots:c.signatureSlots.map((s,n)=>n===i?{...s,...patch}:s)}));
 const addSig=()=>setConfig(c=>({...c,signatureSlots:[...c.signatureSlots,{role:"School Official",name:""}]}));
 return <div className="rc-intel-page"><style>{styles}</style>
  <header className="rc-intel-head"><div><span className="kicker">REPORT CARD INTELLIGENCE</span><h1>Configure the school's academic truth once.</h1><p>SukuuNova normalises every raw mark against its real maximum, applies the school's weighting, calculates positions inside the correct class, and freezes the issued result.</p></div><div className="head-actions"><Link href="/school/report-cards">Back to reports</Link><button className="save" onClick={save} disabled={busy}>{busy?"Saving…":"Save configuration"}</button></div></header>
  {message&&<div className="message">{message}</div>}
  <section className="grid two">
   <article className="card hero-card"><span className="kicker">01 · MARK MODEL</span><h2>Class assessment + examination</h2><p>Example: 30 marks of continuous assessment plus 70 marks of examination = 100 marks overall.</p><div className="weights"><label><span>Class assessment</span><input type="number" min="0" max="100" value={config.classAssessmentWeight} onChange={e=>setConfig(c=>({...c,classAssessmentWeight:Number(e.target.value)}))}/><b>marks</b></label><div className="plus">+</div><label><span>Examination</span><input type="number" min="0" max="100" value={config.examWeight} onChange={e=>setConfig(c=>({...c,examWeight:Number(e.target.value)}))}/><b>marks</b></label><div className={`total ${Math.abs(config.classAssessmentWeight+config.examWeight-100)<0.001?"ok":"bad"}`}>{config.classAssessmentWeight+config.examWeight}<small> / 100</small></div></div></article>
   <article className="card"><span className="kicker">02 · ROUNDING & MISSING MARKS</span><h2>Make edge cases explicit</h2><label className="select"><span>Rounding</span><select value={config.rounding} onChange={e=>setConfig(c=>({...c,rounding:e.target.value as Config["rounding"]}))}><option value="nearest">Nearest</option><option value="up">Round up</option><option value="down">Round down</option></select></label><label className="select"><span>Missing score policy</span><select value={config.missingScorePolicy} onChange={e=>setConfig(c=>({...c,missingScorePolicy:e.target.value as Config["missingScorePolicy"]}))}><option value="blank">Keep result incomplete</option><option value="zero">Treat missing as zero</option></select></label><div className="note">A teacher's work must always have a maximum mark. For example, 17/20 is safely normalised to 85% before the school weighting is applied.</div></article>
  </section>
  <section className="grid two">
   <TypeEditor title="Class assessment types" hint="These work items feed the class-assessment bucket." values={config.classAssessmentTypes} update={i=>updateList("classAssessmentTypes",i)} add={()=>addType("classAssessmentTypes")} remove={i=>removeType("classAssessmentTypes",i)}/>
   <TypeEditor title="Examination types" hint="These labels feed the examination bucket." values={config.examTypes} update={i=>updateList("examTypes",i)} add={()=>addType("examTypes")} remove={i=>removeType("examTypes",i)}/>
  </section>
  <section className="card"><span className="kicker">03 · REPORT PRESENTATION</span><h2>What families see on the official report</h2><div className="toggles"><Toggle label="Student photograph" detail="Show the learner's stored profile photo." checked={config.showStudentPhoto} onChange={v=>setConfig(c=>({...c,showStudentPhoto:v}))}/><Toggle label="Overall class position" detail="Show position and class denominator." checked={config.showOverallPosition} onChange={v=>setConfig(c=>({...c,showOverallPosition:v}))}/><Toggle label="Subject position" detail="Rank the learner within the class for each subject." checked={config.showSubjectPosition} onChange={v=>setConfig(c=>({...c,showSubjectPosition:v}))}/><Toggle label="Attendance" detail="Show present and late information." checked={config.showAttendance} onChange={v=>setConfig(c=>({...c,showAttendance:v}))}/><Toggle label="Promotion decision" detail="Show the configured academic progression status." checked={config.showPromotion} onChange={v=>setConfig(c=>({...c,showPromotion:v}))}/><Toggle label="Class teacher remark" detail="Show the teacher's stored individual remark." checked={config.showClassTeacherRemark} onChange={v=>setConfig(c=>({...c,showClassTeacherRemark:v}))}/><Toggle label="Headteacher remark" detail="Show the approved headteacher remark." checked={config.showHeadteacherRemark} onChange={v=>setConfig(c=>({...c,showHeadteacherRemark:v}))}/></div></section>
  <section className="card"><div className="sig-head"><div><span className="kicker">04 · STORED SIGNATURES</span><h2>Sign once. Reuse until changed.</h2><p>Use a touchscreen, stylus or mouse. The saved signature is attached to future report documents until the school replaces it.</p></div><button onClick={addSig}>+ Add signer</button></div><div className="signatures">{config.signatureSlots.map((sig,i)=><SignatureEditor key={i} value={sig} onChange={p=>updateSig(i,p)} remove={()=>setConfig(c=>({...c,signatureSlots:c.signatureSlots.filter((_,n)=>n!==i)}))}/>)}</div></section>
 </div>;
}

function TypeEditor({title,hint,values,update,add,remove}:{title:string;hint:string;values:string[];update:(i:number,v:string)=>void;add:()=>void;remove:(i:number)=>void}){return <article className="card"><span className="kicker">CONFIGURATION</span><h2>{title}</h2><p>{hint}</p><div className="chips">{values.map((v,i)=><div className="chip" key={`${v}-${i}`}><input value={v} onChange={e=>update(i,e.target.value)}/><button type="button" onClick={()=>remove(i)} aria-label={`Remove ${v}`}>×</button></div>)}</div><button className="add" onClick={add}>+ Add type</button></article>}
function Toggle({label,detail,checked,onChange}:{label:string;detail:string;checked:boolean;onChange:(v:boolean)=>void}){return <label className="toggle"><input type="checkbox" checked={checked} onChange={e=>onChange(e.target.checked)}/><span><strong>{label}</strong><small>{detail}</small></span><i/></label>}
function SignatureEditor({value,onChange,remove}:{value:SignatureSlot;onChange:(p:Partial<SignatureSlot>)=>void;remove:()=>void}){const canvas=useRef<HTMLCanvasElement>(null);const drawing=useRef(false);useEffect(()=>{const c=canvas.current;if(!c)return;const ctx=c.getContext("2d");if(!ctx)return;ctx.clearRect(0,0,c.width,c.height);if(value.signatureDataUrl){const img=new Image();img.onload=()=>ctx.drawImage(img,0,0,c.width,c.height);img.src=value.signatureDataUrl;}},[value.signatureDataUrl]);const point=(e:React.PointerEvent<HTMLCanvasElement>)=>{const c=canvas.current;if(!c)return;const r=c.getBoundingClientRect();return{x:(e.clientX-r.left)*(c.width/r.width),y:(e.clientY-r.top)*(c.height/r.height)}};const down=(e:React.PointerEvent<HTMLCanvasElement>)=>{drawing.current=true;canvas.current?.setPointerCapture(e.pointerId);const p=point(e);const ctx=canvas.current?.getContext("2d");if(ctx&&p){ctx.beginPath();ctx.moveTo(p.x,p.y)}};const move=(e:React.PointerEvent<HTMLCanvasElement>)=>{if(!drawing.current)return;const p=point(e);const ctx=canvas.current?.getContext("2d");if(ctx&&p){ctx.lineWidth=2.4;ctx.lineCap="round";ctx.strokeStyle="#163b38";ctx.lineTo(p.x,p.y);ctx.stroke()}};const up=()=>{drawing.current=false;const data=canvas.current?.toDataURL("image/png");if(data)onChange({signatureDataUrl:data})};return <div className="sig-card"><div className="sig-fields"><label><span>Role</span><input value={value.role} onChange={e=>onChange({role:e.target.value})}/></label><label><span>Typed name</span><input value={value.name} onChange={e=>onChange({name:e.target.value})}/></label><button onClick={remove}>Remove</button></div><canvas ref={canvas} width={720} height={180} onPointerDown={down} onPointerMove={move} onPointerUp={up} onPointerCancel={up} className="sig-pad"/><div className="sig-help"><span>Draw above</span><button onClick={()=>{const c=canvas.current;const ctx=c?.getContext("2d");if(c&&ctx){ctx.clearRect(0,0,c.width,c.height);onChange({signatureDataUrl:""})}}}>Clear signature</button></div></div>}

const styles=`.rc-intel-page{max-width:1250px;margin:0 auto;padding:24px;color:var(--color-text-primary,#163038)}.rc-intel-page *{box-sizing:border-box}.rc-intel-head{display:flex;justify-content:space-between;gap:28px;align-items:flex-start;margin-bottom:16px}.kicker{font-size:10px;font-weight:900;letter-spacing:.15em;text-transform:uppercase;color:var(--color-text-muted,#698084)}.rc-intel-head h1{max-width:780px;margin:7px 0 9px;font-size:36px;line-height:1.02;letter-spacing:-.04em}.rc-intel-head p,.card>p{margin:0;max-width:760px;color:var(--color-text-secondary,#6d8083);font-size:12px;line-height:1.7}.head-actions{display:flex;gap:8px;align-items:center}.head-actions a,.head-actions button,.sig-head button,.add{border:1px solid var(--color-border,#dce7e4);background:var(--color-surface,#fff);color:var(--color-text-primary,#163038);border-radius:12px;padding:10px 13px;text-decoration:none;font-size:10px;font-weight:900}.head-actions .save{background:var(--color-brand,#1aa67e);color:white;border-color:transparent}.message{padding:11px 14px;border:1px solid rgba(26,166,126,.25);background:rgba(26,166,126,.08);border-radius:12px;margin-bottom:14px;font-size:11px;font-weight:800}.grid{display:grid;gap:14px;margin-bottom:14px}.grid.two{grid-template-columns:1.15fr .85fr}.card{border:1px solid var(--color-border,#dce7e4);border-radius:20px;background:var(--color-surface,#fff);box-shadow:0 16px 40px rgba(25,70,64,.05);padding:20px}.card h2{margin:6px 0 9px;font-size:18px;letter-spacing:-.025em}.hero-card{background:radial-gradient(circle at 90% 5%,rgba(26,166,126,.13),transparent 32%),var(--color-surface,#fff)}.weights{display:grid;grid-template-columns:1fr auto 1fr auto;align-items:end;gap:10px;margin-top:18px}.weights label,.select{display:grid;gap:7px}.weights label span,.select span,.sig-fields span{font-size:9px;font-weight:900;letter-spacing:.08em;text-transform:uppercase;color:var(--color-text-muted,#698084)}.weights input{width:100%;height:52px;border:1px solid var(--color-border,#dce7e4);border-radius:13px;padding:0 12px;font-size:22px;font-weight:900;background:var(--color-surface-2,#f7fbfa)}.weights b{font-size:8px;color:#718588}.plus{font-size:22px;font-weight:900;color:#8aa09f;padding-bottom:11px}.total{min-width:88px;height:58px;border-radius:15px;display:grid;place-items:center;font-size:24px;font-weight:950;background:#eef9f5;color:#13745b}.total.bad{background:#fff2f0;color:#b5483b}.total small{font-size:9px;font-weight:800}.select{margin-top:14px}.select select{height:42px;border:1px solid var(--color-border,#dce7e4);border-radius:11px;padding:0 11px;background:var(--color-surface-2,#f7fbfa)}.note{margin-top:14px;padding:11px;border-radius:12px;background:#f6faf9;color:#5f7477;font-size:10px;line-height:1.65}.chips{display:grid;gap:7px;margin-top:14px}.chip{display:flex;gap:7px}.chip input{flex:1;height:39px;border:1px solid var(--color-border,#dce7e4);border-radius:10px;padding:0 10px}.chip button,.sig-fields button,.sig-help button{border:1px solid var(--color-border,#dce7e4);background:var(--color-surface-2,#f7fbfa);border-radius:10px;padding:0 11px;cursor:pointer}.add{margin-top:10px;cursor:pointer}.toggles{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-top:14px}.toggle{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:12px;border:1px solid var(--color-border,#dce7e4);border-radius:13px;background:var(--color-surface-2,#f7fbfa);cursor:pointer}.toggle input{position:absolute;opacity:0}.toggle span{min-width:0}.toggle strong{display:block;font-size:11px}.toggle small{display:block;margin-top:3px;color:#718588;font-size:9px;line-height:1.4}.toggle i{width:34px;height:20px;border-radius:999px;background:#d9e3e1;position:relative;flex:none}.toggle i:after{content:"";position:absolute;top:3px;left:3px;width:14px;height:14px;background:white;border-radius:50%;transition:.18s}.toggle input:checked+i{background:#1aa67e}.toggle input:checked+i:after{left:17px}.sig-head{display:flex;justify-content:space-between;gap:18px;align-items:flex-start}.signatures{display:grid;gap:12px;margin-top:14px}.sig-card{border:1px solid var(--color-border,#dce7e4);border-radius:15px;padding:12px;background:var(--color-surface-2,#f8fbfa)}.sig-fields{display:grid;grid-template-columns:1fr 1fr auto;gap:9px;align-items:end}.sig-fields label{display:grid;gap:6px}.sig-fields input{height:39px;border:1px solid var(--color-border,#dce7e4);border-radius:10px;padding:0 10px;background:var(--color-surface,#fff)}.sig-pad{display:block;width:100%;height:160px;margin-top:10px;border:1px dashed #c9d9d5;border-radius:12px;background:white;touch-action:none}.sig-help{display:flex;justify-content:space-between;gap:10px;margin-top:7px}.sig-help span{font-size:9px;color:#789093}.sig-help button{height:30px;font-size:9px}@media(max-width:900px){.grid.two{grid-template-columns:1fr}.rc-intel-head{flex-direction:column}.head-actions{width:100%}.head-actions>*{flex:1;text-align:center}.toggles{grid-template-columns:1fr}}@media(max-width:600px){.rc-intel-page{padding:14px}.rc-intel-head h1{font-size:28px}.weights{grid-template-columns:1fr}.plus{display:none}.total{min-width:0}.sig-fields{grid-template-columns:1fr}.sig-fields button{height:36px}.card{padding:15px}}`;
