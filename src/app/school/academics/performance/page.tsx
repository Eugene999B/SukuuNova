import Link from "next/link";
import { AppShell } from "@/components/AppShell";
import { AcademicWorkspaceNav } from "@/components/AcademicWorkspaceNav";
import { requireSchoolSession } from "@/lib/school-auth";
import { withTenant } from "@/lib/db";
import { requirePermission, hasPermission } from "@/lib/rbac";
import { getClassSubjectPerformance } from "@/lib/academic-engine";
import { gradeForPercentage, rankTotals } from "@/lib/assessment-engine";
import { isTermActive } from "@/lib/term-date";
import "../../academic-workspace.css";
function positions(rows: Array<{ student: { id: string; name: string }; total: number | null }>) { return rankTotals(rows.filter((row) => row.total != null).map((row) => ({ id: row.student.id, name: row.student.name, total: Number(row.total) }))); }
export default async function PerformanceStudioPage({ searchParams }: { searchParams: Promise<{ class?: string; subject?: string; term?: string }> }) {
  const session = await requireSchoolSession(); const params = await searchParams;
  const data = await withTenant(session.schoolId, async (tx) => {
    const [canGenerateReports, canWriteAll, canWriteAssigned] = await Promise.all([hasPermission(tx, session.userId, "reports:generate"), hasPermission(tx, session.userId, "scores:write:all"), hasPermission(tx, session.userId, "scores:write:assigned")]);
    if (!canGenerateReports && !canWriteAll && !canWriteAssigned) await requirePermission(tx, session.userId, "reports:generate");
    const [school, classes, subjects, terms, assignments, settings] = await Promise.all([
      tx.school.findUnique({ where: { id: session.schoolId }, select: { name: true, uniqueCode: true } }),
      tx.class.findMany({ select: { id: true, name: true, level: true }, orderBy: [{ level: "asc" }, { name: "asc" }] }),
      tx.subject.findMany({ select: { id: true, name: true }, orderBy: { name: "asc" } }),
      tx.term.findMany({ select: { id: true, name: true, startDate: true, endDate: true }, orderBy: { startDate: "desc" } }),
      tx.classSubjectTeacher.findMany({ include: { class: { select: { id: true, name: true, level: true } }, subject: { select: { id: true, name: true } }, teacher: { select: { id: true, name: true } } } }),
      tx.schoolSettings.findUnique({ where: { schoolId: session.schoolId }, select: { timezone: true } }),
    ]);
    const visible = canWriteAll || canGenerateReports ? assignments : assignments.filter((item) => item.teacherId === session.userId);
    const timezone = settings?.timezone || "Africa/Accra";
    const term = terms.find((item) => item.id === params.term) ?? terms.find((item) => isTermActive(item, new Date(), timezone)) ?? null;
    const assignment = visible.find((item) => item.classId === params.class && item.subjectId === params.subject) ?? null;
    const performance = assignment && term ? await getClassSubjectPerformance(tx, assignment.classId, assignment.subjectId, term.id) : null;
    return { school, classes, subjects, terms, term, assignment, performance, canWriteAll, canGenerateReports };
  });
  const rows = data.performance?.rows ?? []; const scored = rows.filter((row) => row.total != null); const average = scored.length ? scored.reduce((sum, row) => sum + Number(row.total), 0) / scored.length : null; const highest = scored.length ? Math.max(...scored.map((row) => Number(row.total))) : null; const ranks = positions(rows); const attention = rows.filter((row) => row.total == null || Number(row.total) < 50); const completePct = rows.length ? Math.round((scored.length / rows.length) * 100) : 0; const contextQuery = data.assignment && data.term ? `?class=${encodeURIComponent(data.assignment.classId)}&subject=${encodeURIComponent(data.assignment.subjectId)}&term=${encodeURIComponent(data.term.id)}` : ""; const classLabel = data.assignment ? `${data.assignment.class.level ? `${data.assignment.class.level} · ` : ""}${data.assignment.class.name}` : "Choose a class"; const subjectLabel = data.assignment?.subject.name ?? "Choose a subject"; const mode = data.canWriteAll || data.canGenerateReports ? "School academic intelligence" : "Your teaching performance";
  return <AppShell universe="school" title="Performance" subtitle="Understand class and subject results." active="Performance Studio" schoolName={data.school?.name ?? "School Workspace"} schoolCode={data.school?.uniqueCode ?? ""} userName={session.name}><div className="academic-page"><section className="academic-page-hero"><div className="academic-page-hero-copy"><span className="academic-page-overline">PERFORMANCE · REVIEW & DECIDE</span><h1>Turn the gradebook into a result picture people can act on.</h1><p>{mode}. The page uses the same class, subject, term and weighted result that teachers work with in Gradebook, so review does not become a second source of truth.</p></div><div className="academic-page-hero-side"><div className="academic-page-context"><span className="academic-context-chip"><strong>Class</strong> {classLabel}</span><span className="academic-context-chip"><strong>Subject</strong> {subjectLabel}</span><span className="academic-context-chip"><strong>Term</strong> {data.term?.name ?? "Choose a term"}</span></div><div className="academic-page-actions"><Link className="academic-btn-secondary" href={`/school/gradebook/studio${contextQuery}`}>Open gradebook</Link><Link className="academic-btn-secondary" href="/school/report-cards">Report cards</Link></div></div></section><AcademicWorkspaceNav current="performance"/><section className="academic-step-row"><div className="academic-step-card"><span className="academic-step-number">01</span><div><strong>Source</strong><small>Academic setup + term</small></div></div><div className="academic-step-card"><span className="academic-step-number">02</span><div><strong>Results</strong><small>Weighted gradebook totals</small></div></div><div className="academic-step-card"><span className="academic-step-number">03</span><div><strong>Decide</strong><small>Find gaps and strong results</small></div></div><div className="academic-step-card"><span className="academic-step-number">04</span><div><strong>Report</strong><small>Carry approved results forward</small></div></div></section><section className="academic-context-card"><div className="academic-section-head"><div><span className="academic-page-overline">WORKING CONTEXT</span><h2>Choose exactly what you want to review</h2><p>Class and subject must be a real teaching assignment. The term comes from the shared academic calendar.</p></div></div><form className="academic-context-form" action="/school/academics/performance" method="get"><div className="academic-field"><label htmlFor="performance-class">Class</label><select id="performance-class" name="class" defaultValue={params.class ?? ""}><option value="">Choose class</option>{data.classes.map((item) => <option key={item.id} value={item.id}>{item.level ? `${item.level} · ` : ""}{item.name}</option>)}</select></div><div className="academic-field"><label htmlFor="performance-subject">Subject</label><select id="performance-subject" name="subject" defaultValue={params.subject ?? ""}><option value="">Choose subject</option>{data.subjects.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div className="academic-field"><label htmlFor="performance-term">Term</label><select id="performance-term" name="term" defaultValue={data.term?.id ?? ""}>{data.terms.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><button className="academic-context-submit" type="submit">Analyse results</button></form></section>{!data.performance ? <section className="academic-empty"><strong>{data.canWriteAll || data.canGenerateReports ? "Select a valid class + subject assignment." : "Select one of your teaching assignments."}</strong><p>{data.term ? "Performance reads the same assignment and term context as Gradebook." : "There is no active academic term. Choose a term explicitly before analysing results."}</p><div className="academic-empty-actions"><Link href="/school/classes">Check class assignments</Link><Link href="/school/gradebook/studio">Enter marks</Link><Link href="/school/terms">Check terms</Link></div></section> : <><section className="academic-stat-row"><div className="academic-stat"><span>Class average</span><strong>{average == null ? "—" : `${average.toFixed(1)}%`}</strong><small>Average of completed weighted results</small></div><div className="academic-stat"><span>Completion</span><strong>{completePct}%</strong><small>{scored.length} of {rows.length} learners have totals</small></div><div className="academic-stat"><span>Highest</span><strong>{highest == null ? "—" : `${highest.toFixed(1)}%`}</strong><small>Strongest completed result</small></div><div className="academic-stat"><span>Needs attention</span><strong>{attention.length}</strong><small>Incomplete or below 50%</small></div></section><section className="academic-main-grid"><div className="academic-work-card"><div className="academic-section-head"><div><span className="academic-page-overline">03 · RESULT TABLE</span><h2 id="performance-results-heading">{subjectLabel} · {classLabel}</h2><p>{data.term?.name} · ranked by the current weighted subject result.</p></div><Link className="academic-btn-secondary" href="/api/school/exports/gradebook.csv">Export results</Link></div><div className="performance-table"><table className="performance-grid" aria-labelledby="performance-results-heading"><thead><tr><th>Position</th><th>Learner</th><th>Result</th><th>Grade</th><th>Status</th></tr></thead><tbody>{rows.map((row) => { const total = row.total == null ? null : Number(row.total); const needsAttention = total == null || total < 50; return <tr key={row.student.id}><td className="performance-rank">{ranks.get(row.student.id) ? `#${ranks.get(row.student.id)}` : "—"}</td><td><div className="performance-name"><strong>{row.student.name}</strong><small>{row.student.admissionNo}</small></div></td><td className="performance-result">{total == null ? "—" : `${total.toFixed(2)}%`}</td><td>{total == null ? "—" : <span className="performance-grade">Grade {gradeForPercentage(total)}</span>}</td><td>{needsAttention ? <span className="performance-attention">Needs attention</span> : <span className="performance-good">On track</span>}</td></tr>; })}</tbody></table></div></div><aside className="academic-side-stack"><div className="academic-side-card"><span className="academic-page-overline">WHAT THIS MEANS</span><strong>One result chain</strong><small>Assessment weights come from Academic Setup and marks come from Gradebook. Performance only interprets that result; it does not redefine grading.</small><div className="academic-link-list"><Link href="/school/academics/setup">Academic setup <span>→</span></Link><Link href="/school/exams">Assessments <span>→</span></Link><Link href={`/school/gradebook/studio${contextQuery}`}>Gradebook <span>→</span></Link></div></div><div className="academic-side-card"><span className="academic-page-overline">ACTION LIST</span><strong>{attention.length ? `${attention.length} learner(s) need review` : "Class is clear"}</strong><small>{attention.length ? "Open Gradebook to complete missing marks or review low results before the term closes." : "There are no incomplete or below-50% results in this context."}</small><div className="academic-link-list"><Link href={`/school/gradebook/studio${contextQuery}`}>Review marks <span>→</span></Link><Link href="/school/academics/term-completion">Term readiness <span>→</span></Link><Link href="/school/report-cards">Prepare reports <span>→</span></Link></div></div></aside></section></>}</div></AppShell>;
}