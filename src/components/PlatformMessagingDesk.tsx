"use client";
import { useEffect, useMemo, useState } from "react";
import { Send, RefreshCw, Search, Check } from "lucide-react";

type School={id:string;name:string;uniqueCode:string};
export default function PlatformMessagingDesk(){
 const [schools,setSchools]=useState<School[]>([]); const [selected,setSelected]=useState<string[]>([]); const [audience,setAudience]=useState("guardians"); const [channel,setChannel]=useState("in_app"); const [userIds,setUserIds]=useState<string[]>([]); const [title,setTitle]=useState(""); const [body,setBody]=useState(""); const [mediaUrl,setMediaUrl]=useState(""); const [busy,setBusy]=useState(false); const [message,setMessage]=useState(""); const [error,setError]=useState(""); const [search,setSearch]=useState("");
 async function load(){const r=await fetch("/api/platform/messaging",{cache:"no-store"});const b=await r.json().catch(()=>({}));if(r.ok)setSchools(b.schools||[]);else setError(b.message||"Unable to load schools.")}
 useEffect(()=>{void load()},[]);
 const visible=useMemo(()=>schools.filter(s=>`${s.name} ${s.uniqueCode}`.toLowerCase().includes(search.toLowerCase())),[schools,search]);
 function toggle(id:string){setSelected(v=>v.includes(id)?v.filter(x=>x!==id):[...v,id])}
 async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMessage("");setError("");const r=await fetch("/api/platform/messaging",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"send",schoolIds:selected,audience,userIds:audience==="individual"?userIds:undefined,channel,title,body,mediaUrl:mediaUrl||undefined})});const b=await r.json().catch(()=>({}));if(!r.ok)setError(b.message||"Unable to send.");else{setMessage(b.message||"Sent.");setTitle("");setBody("");setMediaUrl("");}setBusy(false)}
 return <div className="mx-auto max-w-[1480px] space-y-5 pb-10"><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:justify-between"><div><span className="text-[10px] font-black uppercase tracking-[.16em] text-emerald-600">Platform communication network</span><h1 className="mt-2 text-2xl font-black text-slate-950">Reach the exact schools and people you are responsible for.</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500">Super Admins can message the network. Scoped platform workers can message only their assigned schools. Recipients are resolved from each school’s live staff and guardian accounts.</p></div><button onClick={()=>void load()} className="inline-flex items-center gap-2 self-start rounded-xl border border-slate-200 px-3 py-2 text-[10px] font-black text-slate-700"><RefreshCw size={14}/> Refresh schools</button></div></section><section className="grid gap-5 xl:grid-cols-[.9fr,1.1fr]"><div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="text-sm font-black text-slate-950">01 · Select schools</h2><p className="mt-1 text-[10px] text-slate-500">{selected.length} selected</p></div><button type="button" onClick={()=>setSelected(selected.length===schools.length?[]:schools.map(s=>s.id))} className="text-[10px] font-black text-emerald-700">{selected.length===schools.length?"Clear all":"Select all"}</button></div><label className="mt-4 flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2"><Search size={14} className="text-slate-400"/><input value={search} onChange={e=>setSearch(e.target.value)} className="w-full text-xs outline-none" placeholder="Find a school by name or code…"/></label><div className="mt-3 max-h-[520px] space-y-2 overflow-auto">{visible.map(s=><label key={s.id} className={`flex cursor-pointer items-center gap-3 rounded-xl border p-3 ${selected.includes(s.id)?"border-emerald-500 bg-emerald-50":"border-slate-200"}`}><input type="checkbox" checked={selected.includes(s.id)} onChange={()=>toggle(s.id)} /><span className="min-w-0"><b className="block truncate text-xs text-slate-900">{s.name}</b><span className="text-[9px] text-slate-500">{s.uniqueCode}</span></span></label>)}{!visible.length&&<p className="py-10 text-center text-xs text-slate-400">No schools match that search.</p>}</div></div><form onSubmit={submit} className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-black text-slate-950">02 · Build the message</h2><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="grid gap-1.5"><span className="text-[10px] font-black uppercase tracking-[.08em] text-slate-600">Audience</span><select value={audience} onChange={e=>setAudience(e.target.value)} className="rounded-xl border border-slate-300 px-3 py-3 text-sm"><option value="guardians">Parents / guardians</option><option value="teachers">Teachers</option><option value="staff">All staff</option><option value="all">Everyone</option><option value="individual">Specific people</option></select></label><label className="grid gap-1.5"><span className="text-[10px] font-black uppercase tracking-[.08em] text-slate-600">Channel</span><select value={channel} onChange={e=>setChannel(e.target.value)} className="rounded-xl border border-slate-300 px-3 py-3 text-sm"><option value="in_app">SukuuNova inbox</option><option value="sms">SMS</option><option value="whatsapp">WhatsApp</option></select></label></div>{audience==="individual"?<label className="mt-4 grid gap-1.5"><span className="text-[10px] font-black uppercase tracking-[.08em] text-slate-600">Specific recipient user IDs</span><input value={userIds.join(",")} onChange={e=>setUserIds(e.target.value.split(",").map(v=>v.trim()).filter(Boolean))} className="rounded-xl border border-slate-300 px-3 py-3 text-sm" placeholder="Paste user IDs from the selected schools"/><span className="text-[10px] text-slate-400">Only active users in the selected schools are accepted.</span></label>:null}<label className="mt-4 grid gap-1.5"><span className="text-[10px] font-black uppercase tracking-[.08em] text-slate-600">Subject</span><input required value={title} onChange={e=>setTitle(e.target.value)} className="rounded-xl border border-slate-300 px-3 py-3 text-sm" placeholder="Platform update, training notice, service message…"/></label><label className="mt-4 grid gap-1.5"><span className="text-[10px] font-black uppercase tracking-[.08em] text-slate-600">Message</span><textarea required value={body} onChange={e=>setBody(e.target.value)} rows={9} className="rounded-xl border border-slate-300 px-3 py-3 text-sm leading-6" placeholder="Write exactly what the selected recipients should receive…"/></label>{channel!=="in_app"?<label className="mt-4 grid gap-1.5"><span className="text-[10px] font-black uppercase tracking-[.08em] text-slate-600">Public media URL (optional)</span><input type="url" value={mediaUrl} onChange={e=>setMediaUrl(e.target.value)} className="rounded-xl border border-slate-300 px-3 py-3 text-sm" placeholder="https://…"/></label>:null}<div className="mt-6 flex flex-wrap items-center gap-3"><button disabled={busy||selected.length===0} className="inline-flex items-center gap-2 rounded-xl bg-slate-950 px-5 py-3 text-[11px] font-black text-white disabled:opacity-50"><Send size={14}/>{busy?"Sending…":"Send across selected schools"}</button>{message&&<span className="inline-flex items-center gap-1 text-[10px] font-bold text-emerald-700"><Check size={13}/>{message}</span>}{error&&<span className="text-[10px] font-bold text-rose-700">{error}</span>}</div></form></section></div>;
}
