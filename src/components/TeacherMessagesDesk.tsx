"use client";
import { useEffect, useState } from "react";
import { Check, Inbox, MessageCircle, RefreshCw } from "lucide-react";

type Message={id:string;title:string;body:string;senderName:string;createdAt:string;readAt?:string|null};type Person={id:string;name:string;roles?:string[]};
export default function TeacherMessagesDesk(){const[messages,setMessages]=useState<Message[]>([]);const[recipients,setRecipients]=useState<Person[]>([]);const[unread,setUnread]=useState(0);const[selected,setSelected]=useState("");const[title,setTitle]=useState("");const[body,setBody]=useState("");const[busy,setBusy]=useState(false);const[notice,setNotice]=useState("");const[loading,setLoading]=useState(true);
async function load(){const r=await fetch("/api/teacher/messages",{cache:"no-store"});const b=await r.json().catch(()=>({}));if(r.ok){setMessages(b.messages||[]);setRecipients(b.recipients||[]);setUnread(b.unreadCount||0)}else setNotice(b.message||"Unable to load messages.");setLoading(false)}
useEffect(()=>{void load();const i=window.setInterval(()=>void load(),15000);return()=>window.clearInterval(i)},[]);
async function read(id:string){await fetch("/api/teacher/messages",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"mark_read",messageId:id})});await load()}
async function send(e:React.FormEvent){e.preventDefault();setBusy(true);const r=await fetch("/api/teacher/messages",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"send",recipientId:selected,title,body})});const b=await r.json().catch(()=>({}));setNotice(b.message||"Message could not be sent.");if(r.ok){setTitle("");setBody("");}setBusy(false)}
return <div className="mx-auto max-w-[1200px] space-y-5 pb-10"><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between gap-4"><div><span className="text-[10px] font-black uppercase tracking-[.16em] text-emerald-600">Teacher communication</span><h1 className="mt-2 text-2xl font-black">My messages</h1><p className="mt-2 text-sm text-slate-500">Receive school communication and contact linked guardians directly through the portal.</p></div><button onClick={()=>void load()} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-[10px] font-black"><RefreshCw size={14}/> Refresh</button></div><div className="mt-4 rounded-2xl bg-emerald-50 p-4 text-sm font-black text-emerald-950">{unread} unread message{unread===1?"":"s"}</div></section><section className="grid gap-5 lg:grid-cols-[1.1fr,.9fr]"><section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center gap-2"><Inbox size={17}/><h2 className="text-sm font-black">Inbox</h2></div>{loading?<div className="py-16 text-center"><p className="mt-3 text-sm font-black">Loading messages…</p></div>:messages.length?messages.map(m=><article key={m.id} onClick={()=>!m.readAt&&void read(m.id)} className={`mb-3 cursor-pointer rounded-2xl border p-4 ${m.readAt?"border-slate-200":"border-emerald-200 bg-emerald-50/50"}`}><div className="flex items-start justify-between gap-3"><div><b className="text-xs">{m.title}</b><p className="mt-1 text-[10px] text-slate-500">From {m.senderName} · {new Date(m.createdAt).toLocaleString("en-GH")}</p></div>{!m.readAt&&<span className="rounded-full bg-emerald-600 px-2 py-1 text-[8px] font-black text-white">NEW</span>}</div><p className="mt-3 whitespace-pre-wrap text-xs leading-6 text-slate-700">{m.body.replace(/^.*?\n\n/,"")}</p></article>):<div className="py-16 text-center"><MessageCircle className="mx-auto text-slate-300" size={28}/><p className="mt-3 text-sm font-black">Your inbox is clear.</p></div>}</section><form onSubmit={send} className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-black">Message a linked guardian</h2><p className="mt-1 text-[10px] leading-5 text-slate-500">Teacher-to-family communication is direct and stays inside the school.</p><select required value={selected} onChange={e=>setSelected(e.target.value)} className="mt-4 w-full rounded-xl border border-slate-300 px-3 py-3 text-sm"><option value="">Choose guardian</option>{recipients.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input required value={title} onChange={e=>setTitle(e.target.value)} className="mt-3 w-full rounded-xl border border-slate-300 px-3 py-3 text-sm" placeholder="Subject"/><textarea required value={body} onChange={e=>setBody(e.target.value)} rows={7} className="mt-3 w-full rounded-xl border border-slate-300 px-3 py-3 text-sm leading-6" placeholder="Write your message…"/><button disabled={busy} className="mt-4 inline-flex items-center gap-2 rounded-xl bg-slate-950 px-5 py-3 text-[11px] font-black text-white disabled:opacity-50"><Check size={14}/>{busy?"Sending…":"Send to guardian"}</button>{notice&&<p className="mt-3 text-[10px] font-bold text-emerald-700">{notice}</p>}</form></section></div>}
