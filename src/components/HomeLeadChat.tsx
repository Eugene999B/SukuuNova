"use client";
import { useState } from "react";
export function HomeLeadChat(){const [open,setOpen]=useState(false);const [sent,setSent]=useState(false);const [busy,setBusy]=useState(false);const [form,setForm]=useState({name:"",email:"",phone:"",subject:"",message:""});const submit=async()=>{setBusy(true);const r=await fetch("/api/public/inquiries",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(form)});setBusy(false);if(r.ok){setSent(true);setForm({name:"",email:"",phone:"",subject:"",message:""})}};return <div className="home-chat"><button className="home-chat-button" onClick={()=>setOpen(!open)}>{open?"Close":"Talk to us"}<span>↗</span></button>{open&&<div className="home-chat-panel">{sent?<><div className="home-chat-success">Message received.</div><p>Thanks for reaching out. A real person on the SukuuNova side can pick this up from our inbox.</p><button onClick={()=>setSent(false)}>Send another</button></>:<><div className="home-chat-head"><div><strong>Start a conversation</strong><span>Tell us what you need. We will take it from there.</span></div></div><input placeholder="Your name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input placeholder="Email address" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input placeholder="Phone / WhatsApp" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><input placeholder="What can we help with?" value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})}/><textarea placeholder="Write your message…" rows={4} value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/><button disabled={busy||!form.name||!form.message||(!form.email&&!form.phone)} onClick={submit}>{busy?"Sending…":"Send message"}</button><small>We only need a way to reply to you.</small></>}</div>}</div>}
