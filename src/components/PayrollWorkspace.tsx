"use client";

import { FormEvent, useEffect, useState } from "react";
import Link from "next/link";
import "./finance-workspace.css";

type Staff={id:string;name:string;email?:string|null};
type Structure={id:string;staffId:string;grossSalary:number|string;deductions:unknown;staff?:{name:string}};
type Run={id:string;period:string;status:string};
type Payslip={id:string;staffId:string;gross:number|string;net:number|string;payrollRun?:{period:string}};
type Data={canManage:boolean;staff:Staff[];structures:Structure[];runs:Run[];payslips:Payslip[]};
const money=(n:number|string)=>`GH₵ ${Number(n||0).toLocaleString("en-GH",{minimumFractionDigits:2,maximumFractionDigits:2})}`;

export default function PayrollWorkspace({schoolName}:{schoolName:string}){
 const [data,setData]=useState<Data|null>(null);const [busy,setBusy]=useState(false);const [notice,setNotice]=useState("");
 const [staffId,setStaffId]=useState("");const [gross,setGross]=useState("");const [d1,setD1]=useState("");const [d2,setD2]=useState("");const [d3,setD3]=useState("");const [period,setPeriod]=useState(new Date().toISOString().slice(0,7));
 async function load(){setBusy(true);try{const r=await fetch("/api/phase2/payroll",{cache:"no-store"});if(!r.ok)throw new Error("Payroll could not be loaded.");setData(await r.json());}catch(e){setNotice(e instanceof Error?e.message:"Payroll could not be loaded.");}finally{setBusy(false);}}
 useEffect(()=>{load();},[]);
 async function post(body:unknown){setBusy(true);setNotice("");try{const r=await fetch("/api/phase2/payroll",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j?.error??"Payroll action failed.");setNotice("Saved successfully.");await load();}catch(e){setNotice(e instanceof Error?e.message:"Payroll action failed.");}finally{setBusy(false);}}
 const saveStructure=(e:FormEvent)=>{e.preventDefault();if(!staffId||!gross)return setNotice("Choose a staff member and enter gross salary.");const deductions=[d1,d2,d3].filter(Boolean).map(x=>{const [label,value]=x.split("|");return{label,type:"fixed" as const,value:Number(value)};});post({action:"salaryStructure",staffId,grossSalary:Number(gross),deductions});};
 const salaryTotal=(data?.structures??[]).reduce((s,r)=>s+Number(r.grossSalary),0);
 if(!data)return <div className="finance-app"><div className="finance-loading">{notice||"Loading payroll…"}</div></div>;
 return <div className="finance-app"><section className="finance-hero"><div><span className="finance-kicker">STAFF FINANCE</span><h1>Payroll & Salaries</h1><p>Set salary structures, record approved deductions, run monthly payroll, and keep payslips tied to the correct staff member.</p></div><div className="finance-hero-actions"><Link href="/school/fees">Finance overview</Link><Link href="/school/staff">Staff directory</Link></div></section>{notice&&<div className="finance-notice">{notice}</div>}<section className="finance-metrics"><div><span>Staff on payroll</span><strong>{data.structures.length}</strong><small>Salary structures</small></div><div><span>Gross monthly</span><strong>{money(salaryTotal)}</strong><small>Before deductions</small></div><div><span>Payroll runs</span><strong>{data.runs.length}</strong><small>Saved periods</small></div><div><span>Payslips</span><strong>{data.payslips.length}</strong><small>Generated records</small></div></section>{data.canManage?<section className="finance-grid"><div className="finance-panel"><span className="finance-kicker">SALARY SETUP</span><h2>Set or update salary</h2><p>Choose a staff account and store its recurring gross salary and approved deductions.</p><form className="finance-form" onSubmit={saveStructure}><select value={staffId} onChange={e=>setStaffId(e.target.value)}><option value="">Staff member</option>{data.staff.map(s=><option key={s.id} value={s.id}>{s.name}{s.email?` · ${s.email}`:""}</option>)}</select><input type="number" min="0.01" step="0.01" value={gross} onChange={e=>setGross(e.target.value)} placeholder="Gross salary (GH₵)"/><input value={d1} onChange={e=>setD1(e.target.value)} placeholder="Deduction · e.g. SSNIT|50"/><input value={d2} onChange={e=>setD2(e.target.value)} placeholder="Deduction · e.g. Tax|120"/><input value={d3} onChange={e=>setD3(e.target.value)} placeholder="Optional deduction · e.g. Loan|75"/><button disabled={busy}>Save salary structure</button></form></div><div className="finance-panel finance-accent"><span className="finance-kicker">RUN CONTROL</span><h2>Create monthly run</h2><form className="finance-form" onSubmit={e=>{e.preventDefault();post({action:"createRun",period})}}><input type="month" value={period} onChange={e=>setPeriod(e.target.value)}/><button disabled={busy}>Create run</button></form><p style={{marginTop:12}}>Process a draft to generate payslips. Mark a processed run paid only after the school has completed its payment/reconciliation step.</p></div></section>:<section className="finance-panel"><div className="finance-empty"><strong>Payroll view only</strong><br/>This account can see its authorised payroll information but cannot change school-wide salary structures or runs.</div></section>}<section className="finance-panel"><div className="finance-head"><div><span className="finance-kicker">PAYSLIPS</span><h2>Issued payslips</h2><p>Open a school-branded, print-ready copy for any payslip you are authorised to view.</p></div></div><div className="finance-payroll-list">{data.payslips.map(p=><article key={p.id}><div><b>{p.payrollRun?.period??"Payroll"}</b><span>{money(p.net)}</span></div><Link className="app-action" href={`/school/fees/payroll/payslips/${p.id}/print`}>Print / Save PDF</Link></article>)}{!data.payslips.length&&<div className="finance-empty">No payslips have been issued yet.</div>}</div></section><section className="finance-panel"><div className="finance-head"><div><span className="finance-kicker">PAYROLL REGISTER</span><h2>Salary structures</h2></div></div><div className="finance-table-wrap"><table className="finance-table"><thead><tr><th>Staff</th><th>Gross</th><th>Deductions</th><th>Net estimate</th></tr></thead><tbody>{data.structures.map(s=>{const ds=Array.isArray(s.deductions)?s.deductions as Array<{amount:number|string}>:[];const ded=ds.reduce((n,d)=>n+Number(d.amount||0),0);return <tr key={s.id}><td><b>{s.staff?.name??s.staffId}</b></td><td>{money(s.grossSalary)}</td><td>{money(ded)}</td><td className="paid">{money(Math.max(0,Number(s.grossSalary)-ded))}</td></tr>})}{!data.structures.length&&<tr><td colSpan={4}><div className="finance-empty">No salary structures have been configured yet.</div></td></tr>}</tbody></table></div></section><section className="finance-panel"><div className="finance-head"><div><span className="finance-kicker">RUN HISTORY</span><h2>Payroll runs</h2></div></div><div className="finance-payroll-list">{data.runs.map(r=><article key={r.id}><div><b>{r.period}</b><span>{r.status}</span></div>{data.canManage&&r.status==="draft"&&<button disabled={busy} onClick={()=>post({action:"processRun",payrollRunId:r.id})}>Process</button>}{data.canManage&&r.status==="processed"&&<button disabled={busy} onClick={()=>post({action:"markPaid",payrollRunId:r.id})}>Mark paid</button>}</article>)}{!data.runs.length&&<div className="finance-empty">No payroll runs yet.</div>}</div></section></div>;
}
