"use client";
import { useEffect, useMemo, useState } from "react";
import { BookOpenCheck, CheckCircle2, ChevronRight, Clock3, FileText, GraduationCap, Send, Sparkles } from "lucide-react";

type Student = { id:string; name:string; admissionNo:string; classId:string|null; className:string|null };
type Subject = { id:string; name:string };
type Work = { id:string; subjectName:string; kind:string; title:string; instructions:string|null; workDate:string|Date; weekNumber:number; workNumber:number; maxScore:number|string; markingMode:string; dueAt:string|Date|null; submissionStatus:string; totalAwarded:number|string|null };
type Note = { id:string; title:string; weekNumber:number|null; publishedAt:string|Date|null; content:any; subjectName:string };
type Overview = { students:Student[]; selectedStudent:Student|null; subjects:Subject[]; works:Work[]; notes:Note[] };
type Question = { id:string; position:number; type:string; prompt:string; points:number|string; options:unknown; acceptedAnswers:unknown };
type Activity = { work:Work; submission:any; questions:Question[]; answers:any[] };

async function api(url:string, init?:RequestInit){ const r=await fetch(url,init); const d=await r.json(); if(!r.ok) throw new Error(d?.error??"Request failed"); return d; }
function labelStatus(status:string){ return status==="graded"?"Graded":status==="review_required"?"Awaiting teacher review":status==="in_progress"?"In progress":"Not started"; }

export default function GuardianAcademicWorkspace(){
  const [data,setData]=useState<Overview>({students:[],selectedStudent:null,subjects:[],works:[],notes:[]});
  const [studentId,setStudentId]=useState(""); const [subjectId,setSubjectId]=useState("");
  const [activity,setActivity]=useState<Activity|null>(null); const [answers,setAnswers]=useState<Record<string,string>>({});
  const [loading,setLoading]=useState(true); const [message,setMessage]=useState(""); const [error,setError]=useState("");
  const load=async(sid=studentId,sub=subjectId)=>{ setLoading(true);setError(""); try{const d=await api(`/api/guardian/academic?studentId=${encodeURIComponent(sid)}${sub?`&subjectId=${encodeURIComponent(sub)}`:""}`);setData(d);setStudentId(d.selectedStudent?.id??"");setSubjectId(sub||d.subjects[0]?.id||"");}catch(e){setError(e instanceof Error?e.message:"Could not load academic work");}finally{setLoading(false);} };
  useEffect(()=>{void load();},[]);
  const selectedSubject=useMemo(()=>data.subjects.find(s=>s.id===subjectId),[data.subjects,subjectId]);
  const start=async(workId:string)=>{setError("");try{const d=await api("/api/guardian/academic",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"start",studentId,workId})});setActivity(d);const next:Record<string,string>={};for(const a of d.answers??[]){next[a.questionId]=String(a.responseText??a.responseData??"")}setAnswers(next);}catch(e){setError(e instanceof Error?e.message:"Could not open activity");}};
  const save=async()=>{if(!activity)return;try{await api("/api/guardian/academic",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"save",studentId,workId:activity.work.id,answers:activity.questions.map(q=>({questionId:q.id,responseText:answers[q.id]??""}))})});setMessage("Progress saved for this child.");}catch(e){setError(e instanceof Error?e.message:"Could not save progress");}};
  const submit=async()=>{if(!activity)return;try{await save();const d=await api("/api/guardian/academic",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"submit",studentId,workId:activity.work.id})});setMessage(d.status==="graded"?`Submitted and graded: ${d.totalAwarded}/${d.maxScore}.`:"Submitted. The teacher will review the written responses.");setActivity(null);void load(studentId,subjectId);}catch(e){setError(e instanceof Error?e.message:"Could not submit");}};

  return <div className="gaw-page">
    <section className="gaw-hero"><div><span>GUARDIAN ACADEMIC SPACE</span><h1>{data.selectedStudent?`${data.selectedStudent.name}'s learning stream`:'Academic learning stream'}</h1><p>Assignments, results and teacher notes in one place. Both guardians on the same child see the same work state.</p></div><div className="gaw-hero-icon"><Sparkles size={25}/></div></section>
    {error&&<div className="gaw-alert bad">{error}</div>}{message&&<div className="gaw-alert good">{message}</div>}
    <section className="gaw-context"><label>Child<select value={studentId} onChange={e=>{setStudentId(e.target.value);setSubjectId("");void load(e.target.value,"")}}>{data.students.map(s=><option key={s.id} value={s.id}>{s.name} · {s.admissionNo}</option>)}</select></label><label>Subject<select value={subjectId} onChange={e=>{setSubjectId(e.target.value);void load(studentId,e.target.value)}}><option value="">All subjects</option>{data.subjects.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><div className="gaw-selected">{selectedSubject?.name??"All subjects"} · {data.works.length} activities</div></section>
    <div className="gaw-grid"><section className="gaw-panel"><div className="gaw-head"><div><span>LEARNING WORK</span><h2>Assignments & class activities</h2></div><GraduationCap size={20}/></div><div className="gaw-list">{loading?<p className="gaw-muted">Loading…</p>:data.works.length?data.works.map(w=><article className="gaw-work" key={w.id}><div className="gaw-work-top"><div><span className="gaw-pill">{w.kind}</span><h3>{w.title}</h3><p>Week {w.weekNumber} · Work {w.workNumber} · {String(w.workDate).slice(0,10)} · {w.subjectName}</p></div><div className="gaw-mark">{w.submissionStatus==="graded"?`${w.totalAwarded??0}/${w.maxScore}`:labelStatus(w.submissionStatus)}</div></div>{w.instructions&&<p className="gaw-instructions">{w.instructions}</p>}<div className="gaw-work-actions"><span>{w.dueAt?<><Clock3 size={13}/> Closes {String(w.dueAt).replace('T',' ').slice(0,16)}</>:"No close time"}</span><button onClick={()=>void start(w.id)} disabled={w.submissionStatus==="graded"||w.submissionStatus==="review_required"}><ChevronRight size={15}/>{w.submissionStatus==="in_progress"?"Continue":"Open activity"}</button></div></article>):<div className="gaw-empty"><strong>No published activities yet.</strong><p>Published teacher work will appear here for this child.</p></div>}</div></section>
      <section className="gaw-panel"><div className="gaw-head"><div><span>SUBJECT NOTES</span><h2>Teacher notes</h2></div><BookOpenCheck size={20}/></div><div className="gaw-list">{data.notes.length?data.notes.map(note=><article className="gaw-note" key={note.id}><span>{note.subjectName} · {note.weekNumber?`Week ${note.weekNumber}`:""}</span><h3>{note.title}</h3><div className="gaw-note-content">{Array.isArray(note.content?.blocks)?note.content.blocks.map((b:any,i:number)=>b?.type==="image"&&b?.url?<img key={i} src={b.url} alt="Teacher note illustration"/>:<p key={i}>{String(b?.text??"")}</p>):<p>Published teacher note.</p>}</div></article>):<div className="gaw-empty"><strong>No published notes yet.</strong><p>Teacher-created subject notes will appear here.</p></div>}</div></section></div>
    {activity&&<div className="gaw-overlay"><section className="gaw-modal"><div className="gaw-modal-head"><div><span>{activity.work.kind} · Week {activity.work.weekNumber} · Work {activity.work.workNumber}</span><h2>{activity.work.title}</h2></div><button onClick={()=>setActivity(null)} aria-label="Close">×</button></div>{activity.work.instructions&&<p className="gaw-modal-instructions">{activity.work.instructions}</p>}<div className="gaw-questions">{activity.questions.map(q=><label className="gaw-question" key={q.id}><span>Q{q.position} · {q.points} marks</span><strong>{q.prompt}</strong>{q.type==="multiple_choice"||q.type==="true_false"?<select value={answers[q.id]??""} onChange={e=>setAnswers(v=>({...v,[q.id]:e.target.value}))}><option value="">Choose an answer</option>{(Array.isArray(q.options)?q.options: q.type==="true_false"?["True","False"]:[]).map((o:any,i:number)=><option key={i} value={String(o)}>{String(o)}</option>)}</select>:<textarea rows={5} value={answers[q.id]??""} onChange={e=>setAnswers(v=>({...v,[q.id]:e.target.value}))} placeholder="Write your answer…"/>}</label>)}</div><div className="gaw-modal-actions"><button className="gaw-btn ghost" onClick={()=>void save()}>Save progress</button><button className="gaw-btn primary" onClick={()=>void submit()}><Send size={15}/> Submit activity</button></div><div className="gaw-readonly"><CheckCircle2 size={14}/> After submission, results follow the teacher's marking mode and the same gradebook/report-card chain.</div></section></div>}
    <style>{css}</style>
  </div>;
}

const css=`.gaw-page{display:grid;gap:18px;max-width:1240px;margin:0 auto;padding-bottom:45px}.gaw-hero,.gaw-panel,.gaw-context{border:1px solid var(--sn-line);background:var(--sn-surface);border-radius:var(--sn-radius-xl);box-shadow:var(--sn-shadow-sm)}.gaw-hero{display:flex;justify-content:space-between;gap:20px;align-items:center;padding:28px 32px;background:linear-gradient(135deg,var(--sn-surface),var(--sn-guardian-tint))}.gaw-hero>div:first-child>span,.gaw-head>div>span{font-size:10px;font-weight:900;letter-spacing:.12em;color:var(--sn-guardian-accent)}.gaw-hero h1{font-size:clamp(25px,4vw,38px);margin:6px 0}.gaw-hero p{max-width:760px;margin:0;color:var(--sn-muted);line-height:1.6;font-size:13px}.gaw-hero-icon{width:54px;height:54px;display:grid;place-items:center;border-radius:18px;background:var(--sn-guardian-tint);color:var(--sn-guardian-accent)}.gaw-alert{padding:12px 14px;border-radius:13px;font-size:12px}.gaw-alert.good{background:rgba(47,170,120,.1);color:#087346}.gaw-alert.bad{background:rgba(220,80,80,.1);color:#9e1d1d}.gaw-context{display:grid;grid-template-columns:1fr 1fr auto;gap:10px;padding:16px;align-items:end}.gaw-context label{display:grid;gap:6px;font-size:11px;font-weight:800;color:var(--sn-muted)}.gaw-context select,.gaw-question select,.gaw-question textarea{width:100%;box-sizing:border-box;border:1px solid var(--sn-line);border-radius:10px;background:var(--sn-surface-2);color:var(--sn-ink);padding:11px;font:inherit}.gaw-selected{padding:11px 13px;border:1px solid var(--sn-line);border-radius:10px;font-size:12px;color:var(--sn-muted)}.gaw-grid{display:grid;grid-template-columns:1.35fr 1fr;gap:18px}.gaw-panel{padding:22px}.gaw-head{display:flex;justify-content:space-between;gap:10px;margin-bottom:12px;color:var(--sn-guardian-accent)}.gaw-head h2{margin:4px 0;color:var(--sn-ink);font-size:20px}.gaw-list{display:grid;gap:11px}.gaw-work,.gaw-note{border:1px solid var(--sn-line);background:var(--sn-surface-2);border-radius:16px;padding:16px}.gaw-work-top{display:flex;justify-content:space-between;gap:14px}.gaw-pill{font-size:9px;font-weight:900;letter-spacing:.08em;text-transform:uppercase;color:var(--sn-guardian-accent)}.gaw-work h3,.gaw-note h3{margin:5px 0 4px;font-size:15px;color:var(--sn-ink)}.gaw-work p,.gaw-note p{margin:0;color:var(--sn-muted);font-size:11.5px;line-height:1.55}.gaw-mark{font-size:12px;font-weight:850;color:var(--sn-ink);white-space:nowrap}.gaw-instructions{margin-top:9px!important}.gaw-work-actions{display:flex;justify-content:space-between;align-items:center;gap:10px;margin-top:13px;font-size:10.5px;color:var(--sn-muted)}.gaw-work-actions span{display:flex;align-items:center;gap:5px}.gaw-work-actions button,.gaw-btn{border:1px solid var(--sn-line);border-radius:10px;padding:9px 12px;background:var(--sn-surface);color:var(--sn-ink);font-size:11px;font-weight:800;display:inline-flex;align-items:center;gap:6px;cursor:pointer}.gaw-work-actions button:disabled{opacity:.45;cursor:not-allowed}.gaw-empty{padding:30px 15px;text-align:center;border:1px dashed var(--sn-line);border-radius:14px}.gaw-empty strong{font-size:13px;color:var(--sn-ink)}.gaw-empty p{margin-top:4px}.gaw-note>span{font-size:9px;text-transform:uppercase;letter-spacing:.08em;font-weight:900;color:var(--sn-guardian-accent)}.gaw-note-content{margin-top:10px}.gaw-note-content img{width:100%;max-height:220px;object-fit:cover;border-radius:12px;margin:5px 0}.gaw-note-content p{white-space:pre-wrap;margin:0 0 7px}.gaw-overlay{position:fixed;inset:0;background:rgba(3,8,12,.62);backdrop-filter:blur(5px);display:grid;place-items:center;padding:18px;z-index:100}.gaw-modal{width:min(820px,100%);max-height:90vh;overflow:auto;background:var(--sn-surface);border:1px solid var(--sn-line-strong);border-radius:24px;box-shadow:var(--sn-shadow-md);padding:22px}.gaw-modal-head{display:flex;justify-content:space-between;gap:14px}.gaw-modal-head span{font-size:9px;letter-spacing:.1em;text-transform:uppercase;color:var(--sn-guardian-accent);font-weight:900}.gaw-modal-head h2{margin:5px 0 0}.gaw-modal-head button{border:0;background:transparent;font-size:28px;color:var(--sn-muted);cursor:pointer}.gaw-modal-instructions{color:var(--sn-muted);font-size:12px;line-height:1.6}.gaw-questions{display:grid;gap:12px;margin-top:15px}.gaw-question{display:grid;gap:7px;border:1px solid var(--sn-line);border-radius:15px;padding:14px}.gaw-question>span{font-size:9px;color:var(--sn-guardian-accent);font-weight:900;text-transform:uppercase}.gaw-question strong{font-size:13px;color:var(--sn-ink);line-height:1.45}.gaw-modal-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:16px}.gaw-btn.primary{background:var(--sn-guardian-accent);color:white;border-color:transparent}.gaw-readonly{margin-top:12px;padding:10px 12px;background:var(--sn-surface-2);border-radius:10px;color:var(--sn-muted);font-size:10.5px;display:flex;align-items:center;gap:7px}@media(max-width:820px){.gaw-grid{grid-template-columns:1fr}.gaw-context{grid-template-columns:1fr}.gaw-hero{align-items:flex-start}.gaw-work-top{flex-direction:column}.gaw-mark{align-self:flex-start}}`;
