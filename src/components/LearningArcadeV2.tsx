"use client";

import Link from "next/link";
import { useEffect, useMemo, useRef, useState } from "react";
import { ArrowLeft, ArrowRight, Award, BookOpenCheck, CheckCircle2, ChevronRight, Coins, Gamepad2, Gift, Medal, RefreshCw, Rocket, Search, Sparkles, Star, Trophy, WandSparkles, Zap } from "lucide-react";
import { ArcadePhysicsChoice, ArcadePhysicsOrder, ArcadePhysicsTyped } from "./ArcadePhysicsControls";
import { ArcadePhysicsGrid, ArcadePhysicsMap, ArcadePhysicsMemory, ArcadePhysicsSimulation } from "./ArcadeWorldControls";

type AgeBand="age_4_5"|"age_6_8"|"age_9_11"|"age_12_14"|"age_15_18";
type QuestionKind="choice"|"match"|"sort"|"classify"|"path"|"build"|"typed"|"choice_plus"|"match_plus"|"sort_plus"|"grid"|"map"|"memory"|"simulation";
type Scene={boardTitle?:string;x?:number;y?:number;cells?:string[];meterLabels?:string[];cue?:string};
type Child={id:string;name:string;classId:string|null;class:{name:string;level:string|null}|null};
type Progress={game:string;rounds:number;xp:number;level:number;accuracy:number|null;badges:string[]};
type Game={gameKey:string;name:string;category:string;subject:string;description:string;symbol:string;engine:string;ageBands:AgeBand[];standardBands:string[];difficultyMin:number;difficultyMax:number;roundLengths:number[];defaultRoundLength:number;timerPolicy:string;timedChallengesEnabled:boolean;live:boolean;enabled:boolean;eligible:boolean;dailyGuidanceRounds:number|null;curriculumTags:string[]};
type Overview={children:Child[];selected:Child|null;progress:Progress[];streak:number;recent:Array<{id:string;game:string;correct:number;stars:number;difficulty:number;xp:number;score:number;roundLength:number}>;catalog:Game[];standardBand:string|null;recommendedAgeBand:AgeBand|null;allowedAgeBands:AgeBand[]};
type Question={id:string;kind:QuestionKind;prompt:string;options:string[];scene?:Scene;answer?:string;explanation?:string;correct?:boolean};
type Round={id:string;studentId:string;game:string;difficulty:number;status:string;answers:string[];correct:number|null;xp:number;stars:number;ageBand:AgeBand|null;standardBand:string|null;engine:string;roundLength:number;score:number|null;challengeMode:boolean;questions:Question[]};
type Leaderboard={game:string;scope:string;period:string;standardBand:string;ageBand:AgeBand;rows:Array<{rank:number;studentId:string;displayName:string;bestScore:number;totalXp:number;rounds:number}>};

const ageLabels:Record<AgeBand,string>={age_4_5:"Age 4–5",age_6_8:"Age 6–8",age_9_11:"Age 9–11",age_12_14:"Age 12–14",age_15_18:"Age 15–18"};
const standardLabels:Record<string,string>={kg:"KG",basic_1_3:"Basic 1–3",basic_4_6:"Basic 4–6",jhs:"JHS",shs:"SHS"};
const engineLabel=(value:string)=>value.split("_").map(part=>part.charAt(0).toUpperCase()+part.slice(1)).join(" ");
const gameInitials=(name:string)=>name.split(/\s+/).slice(0,2).map(word=>word[0]).join("").toUpperCase();
async function api(path:string,body?:unknown){const response=await fetch(path,body?{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}:{cache:"no-store"});const data=await response.json();if(!response.ok)throw new Error(data.message??data.error??"Could not connect to the Learning Arcade.");return data;}
function displayAnswer(value:string|undefined,kind:QuestionKind){if(!value)return"—";if(!["sort","build","sort_plus"].includes(kind))return value;try{const parsed=JSON.parse(value);return Array.isArray(parsed)?parsed.join(" → "):value}catch{return value}}

export default function LearningArcadeV2(){
  const [data,setData]=useState<Overview|null>(null);const [round,setRound]=useState<Round|null>(null);const [answers,setAnswers]=useState<string[]>([]);const [index,setIndex]=useState(0);
  const [ageBand,setAgeBand]=useState<AgeBand|"">("");const [category,setCategory]=useState("All");const [query,setQuery]=useState("");const [easier,setEasier]=useState(false);
  const [busy,setBusy]=useState(false);const [loading,setLoading]=useState(true);const [dirty,setDirty]=useState(false);const [error,setError]=useState("");const [message,setMessage]=useState("");
  const [leaderGame,setLeaderGame]=useState<string|null>(null);const [leaderboard,setLeaderboard]=useState<Leaderboard|null>(null);const [leaderLoading,setLeaderLoading]=useState(false);
  const operation=useRef(false);const request=useRef(0);const heading=useRef<HTMLHeadingElement>(null);

  const refresh=async(studentId="")=>{const version=++request.current;setLoading(true);setError("");try{const next=await api(`/api/guardian/arcade?studentId=${encodeURIComponent(studentId)}`) as Overview;if(version===request.current){setData(next);setAgeBand(next.recommendedAgeBand??next.allowedAgeBands[0]??"");setCategory("All");setQuery("");}}catch(loadError){if(version===request.current)setError(loadError instanceof Error?loadError.message:"Could not load the Learning Arcade.");}finally{if(version===request.current)setLoading(false);}};
  useEffect(()=>{void refresh();return()=>{request.current+=1}},[]);
  useEffect(()=>{heading.current?.focus()},[index,round?.id,round?.status]);
  useEffect(()=>{if(!dirty)return;const warn=(event:BeforeUnloadEvent)=>{event.preventDefault();event.returnValue=""};window.addEventListener("beforeunload",warn);return()=>window.removeEventListener("beforeunload",warn)},[dirty]);
  const run=async(action:()=>Promise<void>)=>{if(operation.current)return;operation.current=true;setBusy(true);setError("");setMessage("");try{await action()}catch(actionError){setError(actionError instanceof Error?actionError.message:"Could not save your progress.")}finally{operation.current=false;setBusy(false)}};
  const open=async(body:unknown)=>run(async()=>{const next=await api("/api/guardian/arcade",body) as Round;setRound(next);setAnswers(next.answers);setIndex(0);setDirty(false);setLeaderGame(null);setLeaderboard(null)});
  const save=async(finish:boolean,close=false)=>run(async()=>{if(!round)return;const next=await api("/api/guardian/arcade",{action:"save",roundId:round.id,answers,finish}) as Round;setRound(close?null:next);setDirty(false);setMessage(finish?`Mission complete — ${next.xp} XP earned.`:"Progress saved. You can continue later.");if(finish||close)await refresh(round.studentId)});
  const choose=(value:string)=>{setAnswers(current=>current.map((answer,answerIndex)=>answerIndex===index?value:answer));setDirty(true);setMessage("")};
  const back=()=>{if(round?.status==="in_progress")void save(false,true);else{setRound(null);setMessage("")}};

  const totalXp=useMemo(()=>data?.progress.reduce((sum,item)=>sum+item.xp,0)??0,[data?.progress]);
  const totalRounds=useMemo(()=>data?.progress.reduce((sum,item)=>sum+item.rounds,0)??0,[data?.progress]);
  const novaCoins=Math.floor(totalXp/10)+(data?.recent.reduce((sum,item)=>sum+item.stars*2,0)??0);
  const badges=useMemo(()=>Array.from(new Set((data?.progress??[]).flatMap(item=>item.badges))).filter(Boolean),[data?.progress]);
  const categories=useMemo(()=>["All",...Array.from(new Set((data?.catalog??[]).map(game=>game.category))).sort()],[data?.catalog]);
  const eligible=useMemo(()=>{if(!data||!ageBand)return[];const text=query.trim().toLowerCase();return data.catalog.filter(game=>game.live&&game.enabled&&game.eligible&&game.ageBands.includes(ageBand)&&(!data.standardBand||game.standardBands.includes(data.standardBand))&&(category==="All"||game.category===category)&&(!text||`${game.name} ${game.subject} ${game.category} ${game.description}`.toLowerCase().includes(text)))},[data,ageBand,category,query]);
  const missions=useMemo(()=>[...eligible].sort((a,b)=>{const ap=data?.progress.find(progress=>progress.game===a.gameKey);const bp=data?.progress.find(progress=>progress.game===b.gameKey);return(ap?.rounds??0)-(bp?.rounds??0)}).slice(0,3),[eligible,data?.progress]);
  const current=round?.questions[index];const complete=round?.status==="completed";const answered=answers.filter(answer=>answer.trim()).length;const unfinished=answers.some(answer=>!answer.trim());const roundGame=data?.catalog.find(game=>game.gameKey===round?.game);

  const renderQuestion=(question:Question)=>{
    if(["sort","build","sort_plus"].includes(question.kind))return <ArcadePhysicsOrder items={question.options} value={answers[index]??""} onChange={choose} disabled={busy} mode={question.kind==="build"?"build":"sort"}/>;
    if(question.kind==="typed")return <ArcadePhysicsTyped value={answers[index]??""} onChange={choose} disabled={busy} label="Type your answer"/>;
    if(question.kind==="grid")return <ArcadePhysicsGrid options={question.options} value={answers[index]??""} onChange={choose} disabled={busy} scene={question.scene}/>;
    if(question.kind==="map")return <ArcadePhysicsMap options={question.options} value={answers[index]??""} onChange={choose} disabled={busy} scene={question.scene}/>;
    if(question.kind==="memory")return <ArcadePhysicsMemory options={question.options} value={answers[index]??""} onChange={choose} disabled={busy} scene={question.scene}/>;
    if(question.kind==="simulation")return <ArcadePhysicsSimulation options={question.options} value={answers[index]??""} onChange={choose} disabled={busy} scene={question.scene}/>;
    if(question.kind==="path")return <ArcadePhysicsChoice options={question.options} value={answers[index]??""} onChange={choose} disabled={busy} variant="path" hint="Choose the route that solves the challenge."/>;
    if(question.kind==="match"||question.kind==="match_plus")return <ArcadePhysicsChoice options={question.options} value={answers[index]??""} onChange={choose} disabled={busy} variant="match" hint="Find the strongest match."/>;
    if(question.kind==="classify")return <ArcadePhysicsChoice options={question.options} value={answers[index]??""} onChange={choose} disabled={busy} variant="classify" hint="Place the item in the correct group."/>;
    return <ArcadePhysicsChoice options={question.options} value={answers[index]??""} onChange={choose} disabled={busy} variant="choice" hint="Choose one answer."/>;
  };

  const loadLeaderboard=async(game:Game)=>{if(!data?.selected||!ageBand)return;setLeaderGame(game.gameKey);setLeaderLoading(true);setLeaderboard(null);setError("");try{const params=new URLSearchParams({view:"leaderboard",studentId:data.selected.id,game:game.gameKey,scope:"standard",period:"weekly",ageBand});setLeaderboard(await api(`/api/guardian/arcade?${params.toString()}`) as Leaderboard)}catch(loadError){setError(loadError instanceof Error?loadError.message:"Could not load ranking.")}finally{setLeaderLoading(false)}};

  if(round)return <div className="nova-stage">
    <header className="nova-stage-top"><button type="button" onClick={back} disabled={busy}><ArrowLeft size={15}/>{complete?"Back to Playroom":"Save & leave"}</button><div><span>{roundGame?.name??round.game}</span><strong>{data?.selected?.name??"Learner"} · Level {round.difficulty}</strong></div><div className="nova-stage-progress"><span>{complete?round.questions.length:answered}/{round.questions.length}</span><i><b style={{width:`${round.questions.length?Math.round((complete?round.questions.length:answered)/round.questions.length*100):0}%`}}/></i></div></header>
    {error?<div className="nova-alert" role="alert">{error}</div>:null}
    {complete?<main className="nova-finish"><div className="nova-confetti" aria-hidden="true"><Star/><Sparkles/><Trophy/><Star/><Sparkles/></div><span className="nova-kicker">MISSION COMPLETE</span><h1 ref={heading} tabIndex={-1}>{round.stars===3?"Brilliant run!":round.stars===2?"Strong work!":"Mission cleared!"}</h1><p>{round.correct}/{round.roundLength} correct. Your practice score never changes official school grades.</p><div className="nova-reward-row"><div><Trophy size={22}/><strong>{round.stars}/3</strong><span>Stars</span></div><div><Zap size={22}/><strong>+{round.xp}</strong><span>XP</span></div><div><Coins size={22}/><strong>+{Math.floor(round.xp/10)+round.stars*2}</strong><span>Nova Coins</span></div></div><button className="nova-primary" type="button" onClick={back}>Return to Playroom <ArrowRight size={15}/></button><section className="nova-review"><h2>Mission review</h2>{round.questions.map((question,answerIndex)=><article key={question.id} className={question.correct?"correct":"wrong"}><span>{question.correct?<CheckCircle2 size={15}/>:<RefreshCw size={15}/>}</span><div><strong>{question.prompt}</strong><p>Your answer: <b>{displayAnswer(round.answers[answerIndex],question.kind)}</b>{question.correct?" · Correct":` · Correct answer: ${displayAnswer(question.answer,question.kind)}`}</p>{question.explanation?<small>{question.explanation}</small>:null}</div></article>)}</section></main>:current?<main className="nova-task"><div className="nova-task-head"><span className="nova-kicker">TASK {index+1} OF {round.questions.length} · {engineLabel(round.engine)}</span><h1 ref={heading} tabIndex={-1}>{current.prompt}</h1></div><div className="nova-control">{renderQuestion(current)}</div><footer><button type="button" disabled={busy||index===0} onClick={()=>setIndex(currentIndex=>currentIndex-1)}><ArrowLeft size={14}/>Previous</button><div><button type="button" disabled={busy||!dirty} onClick={()=>void save(false)}>Save checkpoint</button>{index<round.questions.length-1?<button className="nova-primary" type="button" disabled={busy} onClick={()=>setIndex(currentIndex=>currentIndex+1)}>Next challenge<ArrowRight size={14}/></button>:<button className="nova-primary" type="button" disabled={busy||unfinished} onClick={()=>void save(true)}><Rocket size={14}/>{busy?"Scoring…":"Finish mission"}</button>}</div></footer></main>:null}
    <style>{css}</style>
  </div>;

  return <div className="nova-playroom">
    <section className="nova-hero"><div><span className="nova-kicker">SUKUUNOVA NOVA PLAYROOM</span><h1>Learning should feel like entering a world, not opening another worksheet.</h1><p>Short adaptive missions, animated interactions, safe rewards, progress and age-appropriate challenge — all tied to the learner’s school level.</p><div className="nova-hero-actions"><Link href="/guardian"><ArrowLeft size={13}/>Family dashboard</Link>{data?.selected?<button type="button" onClick={()=>void refresh(data.selected!.id)} disabled={loading}><RefreshCw size={13}/>Refresh universe</button>:null}</div></div><div className="nova-orbit" aria-hidden="true"><div><Gamepad2 size={32}/><span>PLAY</span></div><i/><b/></div></section>
    {error?<div className="nova-alert" role="alert">{error}</div>:null}{message?<div className="nova-message" role="status"><Sparkles size={15}/>{message}</div>:null}
    {loading&&!data?<div className="nova-loading"><Gamepad2 size={30}/><strong>Building your learning universe…</strong></div>:!data?.selected?<div className="nova-loading"><UsersRoundFallback/><strong>No linked learner is available for play yet.</strong></div>:<>
      <section className="nova-playerbar"><div className="nova-player-id"><span>{data.selected.name.slice(0,2).toUpperCase()}</span><div><b>{data.selected.name}</b><small>{data.selected.class?.name??"Class not set"} · {standardLabels[data.standardBand??""]??data.standardBand??"Standard not set"}</small></div></div><label>Learner<select value={data.selected.id} disabled={busy||loading} onChange={event=>void refresh(event.target.value)}>{data.children.map(child=><option key={child.id} value={child.id}>{child.name}</option>)}</select></label><div className="nova-wallet"><div><Zap size={15}/><strong>{totalXp}</strong><span>XP</span></div><div><Coins size={15}/><strong>{novaCoins}</strong><span>Nova Coins</span></div><div><Trophy size={15}/><strong>{data.streak}</strong><span>Day streak</span></div><div><Medal size={15}/><strong>{badges.length}</strong><span>Badges</span></div></div></section>

      <section className="nova-mission-zone"><div className="nova-section-head"><div><span className="nova-kicker">TODAY’S MISSION DECK</span><h2>Three good places to start</h2><p>Recommendations prioritize suitable games with less completed practice so the learner keeps exploring.</p></div></div><div className="nova-missions">{missions.map((game,missionIndex)=><MissionCard key={game.gameKey} game={game} progress={data.progress.find(item=>item.game===game.gameKey)} index={missionIndex} busy={busy} onPlay={()=>void open({action:"start",studentId:data.selected!.id,game:game.gameKey,ageBand,easier,roundLength:game.defaultRoundLength})}/>)}</div></section>

      <section className="nova-rewards"><div><span className="nova-kicker">REWARD VAULT</span><h2>Earn, unlock, keep learning.</h2><p>Nova Coins are in-app practice rewards with no cash value. This shelf currently unlocks achievements from verified learning progress.</p></div><div className="nova-reward-shelf"><Reward icon={<Award/>} name="Explorer Crest" threshold={50} xp={totalXp}/><Reward icon={<WandSparkles/>} name="Spark Trail" threshold={150} xp={totalXp}/><Reward icon={<Gift/>} name="Mystery Badge" threshold={300} xp={totalXp}/><Reward icon={<Trophy/>} name="Master Shelf" threshold={600} xp={totalXp}/></div></section>

      <section className="nova-library"><div className="nova-section-head"><div><span className="nova-kicker">GAME LIBRARY</span><h2>Find the right kind of challenge</h2><p>{eligible.length} playable games match the current learner, age band and filters.</p></div></div><div className="nova-filters"><label><Search size={14}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search maths, language, science…"/></label><select value={ageBand} onChange={event=>setAgeBand(event.target.value as AgeBand)}>{data.allowedAgeBands.map(age=><option key={age} value={age}>{ageLabels[age]}{age===data.recommendedAgeBand?" · recommended":""}</option>)}</select><select value={category} onChange={event=>setCategory(event.target.value)}>{categories.map(item=><option key={item}>{item}</option>)}</select><label className="nova-easier"><input type="checkbox" checked={easier} onChange={event=>setEasier(event.target.checked)}/>Gentler next difficulty</label></div><div className="nova-games">{eligible.map(game=><GameCard key={game.gameKey} game={game} progress={data.progress.find(item=>item.game===game.gameKey)} busy={busy} onPlay={()=>void open({action:"start",studentId:data.selected!.id,game:game.gameKey,ageBand,easier,roundLength:game.defaultRoundLength})} onRank={()=>void loadLeaderboard(game)}/>)}</div></section>

      <div className="nova-bottom-grid"><section className="nova-panel"><div className="nova-section-head"><div><span className="nova-kicker">RECENT MISSIONS</span><h2>Progress memory</h2></div></div>{data.recent.length?<div className="nova-history">{data.recent.slice(0,6).map(item=><button type="button" key={item.id} disabled={busy} onClick={()=>void open({action:"view",roundId:item.id})}><span>{data.catalog.find(game=>game.gameKey===item.game)?.symbol??"✦"}</span><div><strong>{data.catalog.find(game=>game.gameKey===item.game)?.name??item.game}</strong><small>{item.correct}/{item.roundLength} correct · {item.stars} stars · +{item.xp} XP</small></div><ChevronRight size={14}/></button>)}</div>:<p className="nova-muted">Finish a mission and its review will stay here.</p>}</section><section className="nova-panel"><div className="nova-section-head"><div><span className="nova-kicker">LEARNING PROFILE</span><h2>{totalRounds} completed missions</h2><p>Badges appear from real Arcade progress, not fabricated scores.</p></div></div><div className="nova-badges">{badges.length?badges.map(badge=><span key={badge}><Medal size={13}/>{badge}</span>):<span><Sparkles size={13}/>First badge is waiting</span>}</div></section></div>

      {leaderGame?<section className="nova-rank"><div className="nova-section-head"><div><span className="nova-kicker">WEEKLY LEADERBOARD</span><h2>{data.catalog.find(game=>game.gameKey===leaderGame)?.name}</h2><p>Privacy-safe ranking inside the learner’s suitable standard band.</p></div><button type="button" onClick={()=>{setLeaderGame(null);setLeaderboard(null)}}>Close</button></div>{leaderLoading?<p>Calculating ranking…</p>:leaderboard?.rows.length?<ol>{leaderboard.rows.map(row=><li key={row.studentId} className={row.studentId===data.selected?.id?"me":""}><strong>#{row.rank}</strong><span>{row.displayName}{row.studentId===data.selected?.id?" · You":""}</span><b>{row.bestScore.toLocaleString()} pts</b><small>{row.totalXp} XP · {row.rounds} rounds</small></li>)}</ol>:<p>No completed rounds in this ranking yet.</p>}</section>:null}
    </>}
    <style>{css}</style>
  </div>;
}

function MissionCard({game,progress,index,busy,onPlay}:{game:Game;progress?:Progress;index:number;busy:boolean;onPlay:()=>void}){return <article className="nova-mission"><div className="nova-mission-rank">0{index+1}</div><GameLogo game={game}/><div><span>{game.category} · {game.subject}</span><h3>{game.name}</h3><p>{game.description}</p><small>Level {progress?.level??1} · {progress?.accuracy==null?"New mission":`${progress.accuracy}% accuracy`}</small></div><button type="button" disabled={busy} onClick={onPlay}><Rocket size={14}/>Launch</button></article>}
function GameCard({game,progress,busy,onPlay,onRank}:{game:Game;progress?:Progress;busy:boolean;onPlay:()=>void;onRank:()=>void}){return <article className="nova-game"><div className="nova-game-top"><GameLogo game={game}/><span>{progress?.rounds?`LV ${progress.level}`:"NEW"}</span></div><h3>{game.name}</h3><p>{game.description}</p><div className="nova-tags"><span>{game.category}</span><span>{engineLabel(game.engine)}</span></div><div className="nova-game-stat"><div><strong>{progress?.accuracy==null?"—":`${progress.accuracy}%`}</strong><span>accuracy</span></div><div><strong>{progress?.xp??0}</strong><span>XP</span></div><div><strong>{game.defaultRoundLength}</strong><span>tasks</span></div></div><footer><button type="button" className="nova-primary" disabled={busy} onClick={onPlay}>Play <ArrowRight size={13}/></button><button type="button" disabled={busy} onClick={onRank}><Trophy size={13}/>Rank</button></footer></article>}
function GameLogo({game}:{game:Game}){return <div className="nova-logo" title={`${game.name} game logo`}><span>{game.symbol}</span><b>{gameInitials(game.name)}</b><i>{game.engine.split("_")[0].slice(0,3).toUpperCase()}</i></div>}
function Reward({icon,name,threshold,xp}:{icon:React.ReactNode;name:string;threshold:number;xp:number}){const unlocked=xp>=threshold;return <div className={unlocked?"unlocked":"locked"}><i>{icon}</i><strong>{name}</strong><span>{unlocked?"Unlocked":`${Math.max(0,threshold-xp)} XP to unlock`}</span></div>}
function UsersRoundFallback(){return <Gamepad2 size={30}/>}

const css=`
.nova-playroom,.nova-stage{max-width:1420px;margin:0 auto;padding-bottom:50px;display:grid;gap:16px;color:var(--sn-ink)}.nova-kicker{font-size:10px;font-weight:900;letter-spacing:.13em;text-transform:uppercase;color:var(--sn-guardian-accent)}.nova-hero{position:relative;overflow:hidden;display:grid;grid-template-columns:1fr 190px;gap:28px;align-items:center;padding:32px;border:1px solid var(--sn-line);border-radius:28px;background:radial-gradient(circle at 88% 20%,var(--sn-guardian-tint),transparent 34%),linear-gradient(140deg,var(--sn-surface),var(--sn-surface-2));box-shadow:var(--sn-shadow-md)}.nova-hero h1{max-width:860px;margin:7px 0 10px;font-size:clamp(30px,4vw,46px);line-height:1.03;letter-spacing:-.04em}.nova-hero p{max-width:780px;margin:0;color:var(--sn-muted);font-size:13px;line-height:1.65}.nova-hero-actions{display:flex;gap:8px;margin-top:18px;flex-wrap:wrap}.nova-hero-actions a,.nova-hero-actions button{display:inline-flex;align-items:center;gap:6px;padding:9px 11px;border:1px solid var(--sn-line);border-radius:10px;background:var(--sn-surface);color:var(--sn-ink);font-size:9.5px;font-weight:850;text-decoration:none;cursor:pointer}.nova-orbit{position:relative;width:170px;height:170px;display:grid;place-items:center;border:1px solid var(--sn-line);border-radius:50%;background:var(--sn-surface)}.nova-orbit:before,.nova-orbit i,.nova-orbit b{content:"";position:absolute;border:1px dashed var(--sn-line);border-radius:50%;animation:nova-spin 14s linear infinite}.nova-orbit:before{inset:13px}.nova-orbit i{inset:31px;animation-direction:reverse}.nova-orbit b{inset:51px}.nova-orbit>div{z-index:2;display:grid;place-items:center;gap:4px;color:var(--sn-guardian-accent)}.nova-orbit span{font-size:9px;font-weight:900;letter-spacing:.15em}@keyframes nova-spin{to{transform:rotate(360deg)}}
.nova-alert,.nova-message{display:flex;align-items:center;gap:8px;padding:11px 13px;border:1px solid var(--sn-line);border-radius:12px;background:var(--sn-surface);font-size:10.5px}.nova-alert{color:var(--color-danger)}.nova-message{color:var(--color-success)}.nova-loading{min-height:280px;display:grid;place-items:center;align-content:center;gap:10px;border:1px dashed var(--sn-line);border-radius:20px;background:var(--sn-surface);color:var(--sn-muted)}
.nova-playerbar{display:grid;grid-template-columns:minmax(220px,1fr) 180px auto;gap:12px;align-items:center;padding:13px;border:1px solid var(--sn-line);border-radius:19px;background:var(--sn-surface)}.nova-player-id{display:flex;align-items:center;gap:10px}.nova-player-id>span{width:42px;height:42px;display:grid;place-items:center;border-radius:13px;background:var(--sn-guardian-tint);color:var(--sn-guardian-accent);font-size:10px;font-weight:900}.nova-player-id div{display:grid}.nova-player-id b{font-size:12px}.nova-player-id small{font-size:9px;color:var(--sn-muted)}.nova-playerbar>label{display:grid;gap:4px;font-size:8.5px;font-weight:850;text-transform:uppercase;color:var(--sn-muted)}.nova-playerbar select,.nova-filters select,.nova-filters input{height:38px;border:1px solid var(--sn-line);border-radius:10px;background:var(--sn-surface-2);color:var(--sn-ink);padding:0 9px;font:inherit;font-size:10px}.nova-wallet{display:flex;gap:7px;justify-content:flex-end}.nova-wallet>div{min-width:76px;padding:8px;border:1px solid var(--sn-line);border-radius:11px;background:var(--sn-surface-2);text-align:center}.nova-wallet svg{color:var(--sn-guardian-accent)}.nova-wallet strong{display:block;font-size:12px}.nova-wallet span{display:block;font-size:7.5px;color:var(--sn-muted);text-transform:uppercase;font-weight:800}
.nova-mission-zone,.nova-library,.nova-rewards,.nova-panel,.nova-rank{padding:20px;border:1px solid var(--sn-line);border-radius:22px;background:var(--sn-surface);box-shadow:var(--sn-shadow-sm)}.nova-section-head{display:flex;justify-content:space-between;align-items:end;gap:12px;margin-bottom:14px}.nova-section-head h2,.nova-rewards h2{margin:4px 0 2px;font-size:20px}.nova-section-head p,.nova-rewards p{margin:0;color:var(--sn-muted);font-size:10.5px;line-height:1.5}.nova-missions{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.nova-mission{position:relative;display:grid;grid-template-columns:auto 1fr;gap:11px;padding:15px;border:1px solid var(--sn-line);border-radius:17px;background:linear-gradient(145deg,var(--sn-surface-2),var(--sn-surface));overflow:hidden}.nova-mission-rank{position:absolute;right:10px;top:4px;font-size:34px;font-weight:950;letter-spacing:-.05em;color:var(--sn-line)}.nova-mission>div:nth-of-type(2){min-width:0}.nova-mission>div:nth-of-type(2)>span{font-size:8px;font-weight:850;color:var(--sn-guardian-accent);text-transform:uppercase}.nova-mission h3{position:relative;margin:5px 0 4px;font-size:13px}.nova-mission p{position:relative;margin:0;color:var(--sn-muted);font-size:9.5px;line-height:1.45}.nova-mission small{display:block;margin-top:7px;color:var(--sn-muted);font-size:8.5px}.nova-mission>button{grid-column:1/-1;display:inline-flex;justify-content:center;align-items:center;gap:5px;padding:8px;border:0;border-radius:9px;background:var(--sn-guardian-accent);color:var(--color-surface);font-size:9.5px;font-weight:850;cursor:pointer}
.nova-logo{position:relative;width:62px;height:62px;display:grid;place-items:center;overflow:hidden;flex:none;border:1px solid var(--sn-line);border-radius:18px;background:radial-gradient(circle at 30% 25%,var(--sn-guardian-tint),var(--sn-surface-2));box-shadow:inset 0 -5px 0 var(--sn-line)}.nova-logo>span{font-size:25px;transform:translateY(-3px)}.nova-logo>b{position:absolute;left:6px;bottom:6px;font-size:8px;letter-spacing:.06em}.nova-logo>i{position:absolute;right:5px;top:5px;padding:2px 4px;border-radius:999px;background:var(--sn-surface);color:var(--sn-muted);font-size:6px;font-style:normal;font-weight:900}
.nova-rewards{display:grid;grid-template-columns:minmax(240px,.55fr) 1.45fr;gap:18px;align-items:center;background:linear-gradient(120deg,var(--sn-surface),var(--sn-guardian-tint))}.nova-reward-shelf{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.nova-reward-shelf>div{display:grid;place-items:center;gap:5px;min-height:105px;padding:10px;border:1px solid var(--sn-line);border-radius:14px;background:var(--sn-surface);text-align:center}.nova-reward-shelf i{width:34px;height:34px;display:grid;place-items:center;border-radius:11px;background:var(--sn-surface-2);color:var(--sn-muted)}.nova-reward-shelf .unlocked i{background:var(--sn-guardian-tint);color:var(--sn-guardian-accent)}.nova-reward-shelf strong{font-size:9.5px}.nova-reward-shelf span{font-size:7.5px;color:var(--sn-muted)}.nova-reward-shelf .locked{opacity:.65}
.nova-filters{display:grid;grid-template-columns:1fr 170px 170px auto;gap:8px;margin-bottom:14px}.nova-filters>label:first-child{display:flex;align-items:center;gap:7px;padding:0 9px;border:1px solid var(--sn-line);border-radius:10px;background:var(--sn-surface-2);color:var(--sn-muted)}.nova-filters>label:first-child input{width:100%;border:0;background:transparent;outline:0}.nova-easier{display:flex;align-items:center;gap:6px;padding:0 10px;border:1px solid var(--sn-line);border-radius:10px;background:var(--sn-surface-2);font-size:9px;color:var(--sn-muted)}.nova-games{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.nova-game{display:flex;flex-direction:column;min-width:0;padding:14px;border:1px solid var(--sn-line);border-radius:16px;background:var(--sn-surface-2);transition:transform .18s ease,border-color .18s ease,box-shadow .18s ease}.nova-game:hover{transform:translateY(-3px);border-color:var(--sn-guardian-accent);box-shadow:0 6px 0 var(--sn-line)}.nova-game-top{display:flex;justify-content:space-between;gap:8px}.nova-game-top>span{height:max-content;padding:4px 6px;border:1px solid var(--sn-line);border-radius:999px;background:var(--sn-surface);font-size:7px;font-weight:900;color:var(--sn-guardian-accent)}.nova-game h3{margin:10px 0 4px;font-size:12.5px}.nova-game>p{min-height:43px;margin:0;color:var(--sn-muted);font-size:9px;line-height:1.45}.nova-tags{display:flex;gap:4px;flex-wrap:wrap;margin:8px 0}.nova-tags span{padding:3px 5px;border:1px solid var(--sn-line);border-radius:999px;background:var(--sn-surface);font-size:7px;color:var(--sn-muted)}.nova-game-stat{display:grid;grid-template-columns:repeat(3,1fr);gap:1px;overflow:hidden;margin-top:auto;border:1px solid var(--sn-line);border-radius:10px;background:var(--sn-line)}.nova-game-stat>div{padding:7px;background:var(--sn-surface)}.nova-game-stat strong{display:block;font-size:9.5px}.nova-game-stat span{font-size:6.8px;text-transform:uppercase;color:var(--sn-muted)}.nova-game footer{display:flex;gap:6px;margin-top:9px}.nova-game footer button,.nova-primary{display:inline-flex;align-items:center;justify-content:center;gap:5px;padding:8px 9px;border:1px solid var(--sn-line);border-radius:9px;background:var(--sn-surface);color:var(--sn-ink);font-size:8.7px;font-weight:850;cursor:pointer}.nova-game footer .nova-primary,.nova-primary{border-color:transparent;background:var(--sn-guardian-accent);color:var(--color-surface)}
.nova-bottom-grid{display:grid;grid-template-columns:1fr .7fr;gap:16px}.nova-history{display:grid;gap:6px}.nova-history button{display:grid;grid-template-columns:34px 1fr auto;gap:9px;align-items:center;text-align:left;padding:9px;border:1px solid var(--sn-line);border-radius:11px;background:var(--sn-surface-2);color:var(--sn-ink);cursor:pointer}.nova-history button>span{width:32px;height:32px;display:grid;place-items:center;border-radius:9px;background:var(--sn-guardian-tint)}.nova-history button div{display:grid}.nova-history strong{font-size:9.5px}.nova-history small{font-size:8px;color:var(--sn-muted)}.nova-badges{display:flex;gap:6px;flex-wrap:wrap}.nova-badges span{display:inline-flex;align-items:center;gap:5px;padding:7px 9px;border:1px solid var(--sn-line);border-radius:999px;background:var(--sn-guardian-tint);color:var(--sn-guardian-accent);font-size:8.5px;font-weight:800}.nova-muted{color:var(--sn-muted);font-size:10px}.nova-rank .nova-section-head>button{padding:7px 9px;border:1px solid var(--sn-line);border-radius:8px;background:var(--sn-surface-2);color:var(--sn-ink);cursor:pointer}.nova-rank ol{list-style:none;padding:0;margin:0;display:grid;gap:6px}.nova-rank li{display:grid;grid-template-columns:40px 1fr auto auto;gap:10px;align-items:center;padding:9px;border:1px solid var(--sn-line);border-radius:10px;background:var(--sn-surface-2);font-size:9px}.nova-rank li.me{border-color:var(--sn-guardian-accent);background:var(--sn-guardian-tint)}.nova-rank li small{color:var(--sn-muted)}
.nova-stage{position:fixed;inset:0;z-index:1000;max-width:none;margin:0;padding:0;background:radial-gradient(circle at 75% 10%,var(--sn-guardian-tint),transparent 30%),var(--sn-bg);overflow:auto}.nova-stage-top{position:sticky;top:0;z-index:10;display:grid;grid-template-columns:180px 1fr 260px;gap:15px;align-items:center;padding:14px 22px;border-bottom:1px solid var(--sn-line);background:color-mix(in srgb,var(--sn-surface) 94%,transparent);backdrop-filter:blur(14px)}.nova-stage-top>button{display:inline-flex;align-items:center;gap:5px;width:max-content;padding:8px 10px;border:1px solid var(--sn-line);border-radius:9px;background:var(--sn-surface-2);color:var(--sn-ink);font-size:9px;font-weight:800;cursor:pointer}.nova-stage-top>div:nth-child(2){display:grid;text-align:center}.nova-stage-top span{font-size:8px;color:var(--sn-muted)}.nova-stage-top strong{font-size:10px}.nova-stage-progress{display:flex!important;align-items:center;gap:8px;text-align:left!important}.nova-stage-progress>i{flex:1;height:7px;overflow:hidden;border-radius:99px;background:var(--sn-surface-2)}.nova-stage-progress b{display:block;height:100%;border-radius:inherit;background:var(--sn-guardian-accent);transition:width .25s ease}.nova-task{width:min(980px,calc(100% - 40px));min-height:calc(100vh - 76px);margin:0 auto;display:grid;grid-template-rows:auto 1fr auto;gap:20px;padding:36px 0}.nova-task-head{text-align:center}.nova-task-head h1{max-width:850px;margin:8px auto;font-size:clamp(24px,3vw,38px);line-height:1.18;letter-spacing:-.025em}.nova-control{align-self:center;padding:24px;border:1px solid var(--sn-line);border-radius:24px;background:var(--sn-surface);box-shadow:var(--sn-shadow-md)}.nova-task>footer{display:flex;justify-content:space-between;gap:10px;padding:12px;border:1px solid var(--sn-line);border-radius:14px;background:var(--sn-surface)}.nova-task>footer>div{display:flex;gap:6px}.nova-task>footer button{display:inline-flex;align-items:center;gap:5px;padding:9px 11px;border:1px solid var(--sn-line);border-radius:9px;background:var(--sn-surface-2);color:var(--sn-ink);font-size:9px;font-weight:850;cursor:pointer}.nova-task>footer button.nova-primary{border-color:transparent;background:var(--sn-guardian-accent);color:var(--color-surface)}.nova-finish{width:min(980px,calc(100% - 40px));margin:0 auto;padding:48px 0;text-align:center}.nova-confetti{display:flex;justify-content:center;gap:18px;color:var(--sn-guardian-accent);animation:nova-pop .7s cubic-bezier(.2,1.4,.3,1)}@keyframes nova-pop{0%{transform:scale(.4);opacity:0}100%{transform:scale(1);opacity:1}}.nova-finish h1{margin:9px 0 5px;font-size:clamp(32px,5vw,58px);letter-spacing:-.05em}.nova-finish>p{color:var(--sn-muted)}.nova-reward-row{width:min(600px,100%);margin:24px auto;display:grid;grid-template-columns:repeat(3,1fr);gap:9px}.nova-reward-row>div{padding:18px;border:1px solid var(--sn-line);border-radius:16px;background:var(--sn-surface)}.nova-reward-row svg{color:var(--sn-guardian-accent)}.nova-reward-row strong{display:block;margin-top:4px;font-size:22px}.nova-reward-row span{font-size:8px;text-transform:uppercase;color:var(--sn-muted)}.nova-review{margin-top:36px;text-align:left;padding:20px;border:1px solid var(--sn-line);border-radius:20px;background:var(--sn-surface)}.nova-review h2{margin:0 0 12px}.nova-review article{display:grid;grid-template-columns:32px 1fr;gap:9px;padding:11px;border:1px solid var(--sn-line);border-radius:11px;background:var(--sn-surface-2);margin-top:7px}.nova-review article>span{width:30px;height:30px;display:grid;place-items:center;border-radius:9px;background:var(--sn-surface)}.nova-review article.correct>span{color:var(--color-success)}.nova-review article.wrong>span{color:var(--color-warning)}.nova-review strong{font-size:10.5px}.nova-review p{margin:3px 0;font-size:9.5px}.nova-review small{color:var(--sn-muted);font-size:9px;line-height:1.5}
@media(max-width:1100px){.nova-games{grid-template-columns:repeat(3,1fr)}.nova-wallet{display:grid;grid-template-columns:1fr 1fr}.nova-playerbar{grid-template-columns:1fr 170px auto}.nova-missions{grid-template-columns:1fr}.nova-rewards{grid-template-columns:1fr}.nova-stage-top{grid-template-columns:150px 1fr 190px}}
@media(max-width:760px){.nova-hero{display:block;padding:22px}.nova-orbit{display:none}.nova-playerbar{grid-template-columns:1fr}.nova-wallet{grid-template-columns:repeat(4,1fr);justify-content:stretch}.nova-filters{grid-template-columns:1fr 1fr}.nova-filters>label:first-child{grid-column:1/-1}.nova-games{grid-template-columns:1fr 1fr}.nova-reward-shelf{grid-template-columns:1fr 1fr}.nova-bottom-grid{grid-template-columns:1fr}.nova-stage-top{grid-template-columns:1fr auto}.nova-stage-progress{grid-column:1/-1}.nova-stage-top>div:nth-child(2){text-align:right}.nova-task>footer{align-items:flex-start;flex-direction:column}.nova-task>footer>div{width:100%;justify-content:flex-end;flex-wrap:wrap}}
@media(max-width:500px){.nova-wallet{grid-template-columns:1fr 1fr}.nova-games{grid-template-columns:1fr}.nova-filters{grid-template-columns:1fr}.nova-reward-row{grid-template-columns:1fr}.nova-task{width:calc(100% - 24px);padding:22px 0}.nova-control{padding:14px}.nova-stage-top{padding:10px 12px}}
@media(prefers-reduced-motion:reduce){.nova-orbit:before,.nova-orbit i,.nova-orbit b,.nova-confetti{animation:none}.nova-game{transition:none}.nova-game:hover{transform:none}}
`;
